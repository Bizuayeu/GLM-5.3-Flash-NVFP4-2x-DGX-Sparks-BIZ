# TP=2ベンチマークの方法

[English](benchmarks.md) · [検証範囲](validation.ja.md)

本書はTP=2ベンチの方法、MTPなしの基準値、独立施策の全モデル実行（P08・P11・P13〜P15・P17〜P19・P21・P22・checkpoint保持）、リリース候補・200K・256Kの実入力確認と、それを1.3.1で繰り返したsparkDash・200Kの測定を所有します。MTP k=1／k=3の比較は[投機的デコーディング](speculative-decoding.ja.md)、現在の画像入力の既定は[画像入力](vision.ja.md)を参照してください。

まず動作を確認したprofileを測り、その後にkernelや高速化設定を変えます。数値はイメージ・精度・scheduler・負荷条件に依存し、本番信頼性やハーネス連携の合格を意味しません。

## 初期の測定条件

GB10 2台、TP=2、Marlin W4A16、全候補保持の参照attention、eager、MTP/APCなし、context 16,384、各GPUのKV予算1 GiBを対象とします。サーバーの`max_num_seqs=1`を明示します。fixtureでは同時実行2で僅差の候補が同点となりtoken列が変わったため、そのprofileは未検収です。

| ケース | 要求入力token | 出力token | クライアント同時数 | 測定要求数 |
|---|---:|---:|---:|---:|
| 短文 | 32 | 64 | 1 | 3 |
| 中程度 | 2,048 | 64 | 1 | 3 |
| 長文 | 8,192 | 64 | 1 | 3 |
| 短文・待ち行列 | 32 | 64 | 2 | 6 |
| 中程度・待ち行列 | 2,048 | 64 | 2 | 6 |

クライアント2同時は、サーバーの2要求同時演算ではなく待ち行列を含む測定です。固定イメージ内の公式`vllm bench serve`のrandom datasetをseed固定で使用します。言語品質の評価ではありません。指定長だけでなく、結果JSON内の実入力・実出力token数も残します。

## 実行方法

試験サーバーと**同一のイメージ**で実行します。ベンチ用クライアントにはGPUを割り当てず、検証済みtokenizerをローカルmountし、Hugging Faceのoffline設定を使って別のtokenizerやdatasetを取得しないようにします。引数はそのイメージの`vllm bench serve --help=all`で確認し、別に導入されたリリースのhelpは使いません。

1ケース分のベンチ引数は次のとおりです。

```sh
vllm bench serve \
  --backend vllm --base-url http://127.0.0.1:8891 \
  --endpoint /v1/completions --model glm-5.3-flash-nvidia \
  --tokenizer /path/to/verified/snapshot \
  --dataset-name random --random-input-len 32 --random-output-len 64 \
  --random-range-ratio 0 --num-prompts 3 --num-warmups 1 \
  --request-rate inf --max-concurrency 1 --ignore-eos --seed 42 --temperature 0 \
  --percentile-metrics ttft,tpot,itl,e2el --metric-percentiles 50 \
  --save-result --save-detailed --result-dir /path/to/private/results \
  --result-filename short-c1.json
```

パスは例示です。入力長・出力長・同時数は測定条件の表の値をケースごとに当てはめ、warmupを1回分指定し、新しい出力先・コンテナ名を使います。`ignore_eos`は出力長を固定する速度測定用であり、通常会話の設定ではありません。起動ガードを回避するためのコマンドではなく、正常に起動した試験endpointが前提です。

CLIが終了コード0でも完了要求数0の場合があります。[結果検査器](../tools/assess_benchmark.py)で、完了数・出力token数・有限かつ正の指標を独立に確認します。

```sh
python tools/assess_benchmark.py /path/to/private/results/short-c1.json --requests 3 --output-tokens 64
```

起動と計測には別の期限を設け、API readyを確認したら自動で計測段階へ切り替えます。長いロードが計測の時間枠を食い潰さないようにするためです。

## 記録する指標

- 起動時間: コンテナ開始からAPI readyまで。重みロード・コンパイルを含め、TTFTとは別にする。
- TTFT: クライアントが最初のstream tokenを受けるまで。要求処理・待ち行列・prefillを含む。
- TPOT／ITL: 公式クライアントの値と実出力長を記録する。TPOTから換算したdecode速度と、全要求の出力throughputは区別する。
- 全体throughput: 測定区間に完了した出力tokenの合計／秒。単一要求の生成速度とは呼ばない。
- E2E遅延: 要求ごとの値を保存し、取得できるclient/server待ち時間も区別する。
- メモリ: 各機の利用可能メモリ最小値、コンテナのcurrent/peak/eventsを記録する。cgroupの値は重みbytesそのものではなく、GPU割当をすべて含むとも限らないため、ランタイムとホスト側の観測を併用する。各記録のコンテナ上限とhost reserveはそのrun時点の条件であり、配布既定は[examples/server.example.toml](../examples/server.example.toml)が正典。
- 失敗: 全要求の完了と出力token数を確認し、エラー・短すぎる終了・中断・OOMを別記する。warmupや初回コンパイルを定常値に混ぜない。

実行前にDocker以外のホスト作業とfabric通信も確認します。他作業を勝手に止めず、共有負荷が残る場合は明記します。初期の少数サンプルはbaseline・中央値の確認用で、本番p95/p99 SLAの証明には使いません。速度と併せて実文の応答、SSE、ツール往復、[ハーネス受け入れ](harnesses.ja.md)を検査します。

CLIの説明は[vLLM公式資料](https://docs.vllm.ai/en/latest/cli/bench/serve/)を参照し、実行コマンド・help・生結果は固定ランタイムのものを記録してください。

## 全モデルの初期結果

5ケース、測定対象21要求がすべて完了し、各64出力tokenと独立した結果検査を通過しました。TTFTは中央値、decode換算値は`1000 / mean_tpot_ms`、全体出力は公式クライアントのthroughputです。

| 入力token | クライアント同時数 | 初動中央値（秒） | decode換算（token/s） | 全体出力（token/s） |
|---:|---:|---:|---:|---:|
| 32 | 1 | 0.256 | 14.29 | 13.71 |
| 2,048 | 1 | 6.092 | 14.15 | 6.06 |
| 8,192 | 1 | 24.387 | 13.96 | 2.21 |
| 32 | 2 | 4.893 | 14.27 | 13.71 |
| 2,048 | 2 | 16.573 | 14.22 | 6.08 |

上記profileでの結果です。ソースcommit `1816722`、固定ベースから構築した参照image ID `sha256:c4f0aa51b70b85ec86cdf12a25468a443475dfe5b67216c92f8bd906df22b0c3`を使用しました。イメージはレジストリへ公開していません。クライアント2同時では待ち時間が増えますが、サーバーは1 sequenceなので、並列バッチ性能の証明ではありません。この範囲では長い入力のprefill待ちが主な遅延増加要因です。

vLLMの報告するモデル用メモリは各rank約88.2 GiB。ベンチrun中のホスト利用可能メモリは各台で約11.6／12.5 GiB以上を維持し、OOMはありませんでした。最大性能・本番品質を保証する値ではなく、限定条件の合成ベンチbaselineです。両ハーネスの実接続は未検証です。

## 標準batchingの独立評価

2026-09-12（Asia/Tokyo）に、サーバーの`max_num_seqs`だけを **1 → 2 → 1** と変更してA/B/Aを実施しました。全条件でsource `eb30095`、image `sha256:e18f7ae02e96beeb9af954a4e5600ce6ff534d9f91a9fcc2e440a4d91350c494`、TP=2・Marlin W4A16・FP8 KV各1 GiB・context 16,384・chunk 512を固定。MTP/LPA/fusion/Graphs/APCとtraceは無効です。固定ランタイムのrandomベンチをseed 42、warmup 1回、出力64token、クライアント同時数あたり3要求で測定。各条件の測定18要求がすべて完了し、出力数と有限値の検査を通過しました。非公開runは`batching-v14-serial`／`batching-v14-batch2`／`batching-v14-restored`です。

**容量と測定範囲:** KV予算は各rankの共有poolに1 GiB固定で、同時数に掛けて増額したものではありません。速度比較は**入力2,048＋出力64＝2,112tokenまでを最大2要求同時**で実施しました。別の[最大長での容量確認](#最大長での容量確認)では16,384token×2要求も完了しました。起動ログの58,254token／最大長で3.56並列という計算値と、実測した対応範囲を区別します。[KV容量とRAMの条件](server-configuration.ja.md#kv容量とramの条件)

prefillと待ち行列を含む全体出力（token/s）:

| 入力token | クライアント同時数 | サーバー1系列 | サーバー2系列 | 復帰後1系列 |
|---:|---:|---:|---:|---:|
| 32 | 1 | 13.477 | 13.677 | 13.826 |
| 32 | 2 | 13.656 | 24.347 | 13.796 |
| 2,048 | 1 | 6.072 | 6.068 | 6.047 |
| 2,048 | 2 | 6.067 | 7.628 | 6.069 |

クライアント2同時の実running数は、サーバー2系列の条件だけ2になりました。短文のengine-step histogramでは2〜8tokenを処理したstepが183回、両1系列対照では0回です。MTPなし・prefillは32token固定という条件と合わせ、実際のdecode batchingを確認しました。クライアントを2本開いただけの状態と区別します。running gaugeは250ms間隔、engine-step counterは前後を保存しています。

クライアント2同時の遅延（TTFT中央値／要求ごとの平均token間隔であるTPOTの中央値）:

| 入力token | サーバー1系列 | サーバー2系列 | 復帰後1系列 |
|---:|---:|---:|---:|
| 32 | 4.929秒／70.18ms | 0.489秒／74.20ms | 4.892秒／69.59ms |
| 2,048 | 16.645秒／70.99ms | 7.005秒／153.33ms | 16.653秒／70.55ms |

2KのTPOT増加には他要求のprefillによる待ちが含まれ、すべてのdecode間隔が倍になったという意味ではありません。ITL中央値は2系列73.27ms、両1系列対照70.73／70.13msでした。長い停止が要求ごとの平均を押し上げているため、個々の間隔も保存します。少数要求の結果をSLAには使いません。

各条件で、計算・資料抽出4問をクライアント同時数1／2で実行した計8回答と、異なる注文ID・確認コードによる並列tool往復2件が期待値に一致しました。限定的なタスク確認であり、一般品質・cancel・テナント分離の検収ではありません。全条件を通じた空きメモリはrank 0で11.4 GiB以上、rank 1で12.5 GiB以上。試験ガードは4 GiBで、OOMはありません。制御停止時のrank 0はexit 0、rank 1は従来どおり強制終了（exit 137、OOMではない）が必要でした。

**判断:** 上記の入力32／2,048・出力64token、最大2同時要求の範囲で、独立throughput用の2系列を受け入れます。全体出力は両1系列対照比、短文で約76〜78%、2Kで約26%改善しました。既定と単発遅延の対照は1系列を維持します。LPAはこの複数系列profileと非対応です。MTP/fusion/Graphsとの併用、4系列、長文の品質・性能、連続負荷、復旧は別検収であり、業務利用や通常serviceの認定を意味しません。

### 最大長での容量確認

同じ2系列profile・各rank 1 GiB KVで、非公開run `capacity-v14-16kx2` を追加しました。LLM-jp validation streamの異なる2区間をtoken IDsで入力し、各要求が**16,320入力＋64出力＝16,384token**を完了しました。合計の要求予算は32,768tokenです。APCは無効で、prefix共有に依存していません。

| 確認項目 | 実測 |
|---|---|
| 完了要求・実usage | 2／2要求、両方16,384token |
| 実行系列数の最大 | 2 |
| KV使用率の最大 | 約58.1% |
| preemption増分 | 0 |
| 終了後 | KV使用率0、後続の32入力＋1出力要求も完了 |
| 空きメモリ最小 | rank 0 約11.64 GiB、rank 1 約12.83 GiB |

この固定構成では16K×2を収容できたという容量の証拠です。出力64tokenは`ignore_eos`で固定した容量試験であり、16K文書の理解品質・定常速度、別の入力／出力配分、MTP等を加えた構成を保証しません。KV量・context・同時数・cache形式・runtimeを変える場合は再確認します。

## Prefill chunk の独立評価（P11）

2026-09-12〜13（Asia/Tokyo）、上記2系列の条件を固定し、`max_num_batched_tokens`だけを512→128→1024→512と比較しました。これはschedulerのtoken予算であり、attention内部のquery chunkとは別です。各条件の32／2,048入力×クライアント1／2、出力64tokenの全18要求、計算・抽出8回答、並列tool往復2件が完了・合格しました。イメージと実行ソースはP13と同じです。追加runは`batching-v14-chunk128`／`batching-v14-chunk1024`／`batching-v14-chunk512restore`です。

全体出力（token/s）:

| 入力token | クライアント同時数 | 512対照 | 128 | 1024 | 512復帰 |
|---:|---:|---:|---:|---:|---:|
| 32 | 1 | 13.677 | 13.663 | 13.547 | 13.590 |
| 32 | 2 | 24.347 | 24.313 | 24.307 | 24.724 |
| 2,048 | 1 | 6.068 | 4.560 | 6.347 | 6.068 |
| 2,048 | 2 | 7.628 | 5.023 | 8.062 | 7.623 |

2K・クライアント2同時の遅延:

| 指標 | 512対照 | 128 | 1024 | 512復帰 |
|---|---:|---:|---:|---:|
| TTFT中央値（秒） | 7.005 | 13.363 | 7.132 | 7.023 |
| TPOT中央値（ms） | 153.33 | 200.31 | 138.46 | 153.51 |
| ITL p95（ms） | 107.23 | 602.08 | 80.44 | 110.13 |
| 観測した最長ITL（秒） | 1.551 | 0.687 | 2.790 | 1.562 |

**判断:** 128は最長停止を短縮しましたが、prefillと全体出力が悪化したため不採用です。1024は2Kの全体出力を両512対照比で約4.6%（単発）／5.7%（2同時）改善し、単発TTFTも約6.1→5.6秒に短縮しました。ただし2同時の最長停止は約1.56→2.79秒に延びました。**1024はこの停止を許容するthroughput試験の採用候補**とし、既定512は維持します。p95改善だけから対話の途切れも改善したとはしません。少数測定であり、SLAや長文の定常性能を保証しません。

1024の別容量run `capacity-v17-chunk1024-16kx2` でも、各16,320入力＋64出力の2要求が完了し、実running最大2、KV最大約58.1%、preemption増分0、終了後KV使用0、後続要求の完了を確認しました。固定KVは各rank 1 GiBのままです。LPA/MTP/fusion/Graphs/APCとの併用と、長文内容の品質は別検収です。

### 200K画像profileでのchunk予算（2026-09-17）

配布用1.3.1のprofile（200K・画像入力・rankあたりKV 2.5 GiB・MTP k=3・APC・unpack融合・同時1系列・NCCL 8チャネル・MTU 1500）を、source `adf8ca9`・image `f6fc154c…` で起動ごとに `max_num_batched_tokens` だけ変えて測りました。headの監視ダッシュボードは止めています。prefillはprefix cacheをresetした約39K tokenの新規prompt、decodeは固定promptで512 token生成、それぞれ3回です。最小空きは各rankの監視プロセスの2秒標本です。着想はMiaの300Kの記録（別runtimeで1024→2048が+1.4%、AGPL-3.0、コードは採用しない）です。

| chunk | prefill tok/s（中央値・範囲） | decode tok/s（中央値・範囲） | headの最小空き 起動／計測（GiB） | peerの最小空き（GiB） | warmupの長文段 65,566 token |
|---:|---|---|---|---|---:|
| 512 | 476.9（469.2〜478.6） | 31.4（22.4〜33.9） | 7.09／7.30 | 9.45／9.54 | 139.3秒 |
| 1024 | 545.2（545.0〜546.9） | 27.4（23.7〜28.7） | 6.71／6.73 | 9.39／9.43 | 121.6秒 |
| 2048 | 563.0（561.9〜563.7） | 28.0（21.5〜28.4） | 6.63／6.71 | 8.70／8.77 | 123.4秒 |

2048では、[1.3.1での200K実入力](#131での200k実入力)と同じ199,652 tokenの合言葉要求が、512の410.8秒に対して361.3秒で、正答し正常に終了しました。その要求中の最小空きはhead 6.88 GiB・peer 9.16 GiBで、512では7.12・9.44でした。decodeの差は実行ごとのばらつきの範囲内です。1024と2048は655 tokenを超える最初の予算でindexerの分割経路を使いますが、どの大きさでも配信中のkernelのコンパイルはありませんでした。2048の起動ではpeerで `NV_ERR_NO_MEMORY` の再試行が28件、すべて重みのロード中に記録されました（1024では0件）。起動とすべての要求は完了しています。512のprefillは、ダッシュボード稼働中に測った1.3.1の492.0 tok/sより約3%低い値です。再起動をまたぐため、原因は切り分けていません。

**判断:** 1.4.0で配布の既定を2048にします。既定の同時1系列では、chunkを長くしても他の要求は待たされません。2系列では、上の1024の行が示したように停止が長くなると見込んでください。2048を超える大きさは測っていません。Miaの記録では、indexerの共有メモリの上限は4096付近です。

## 同種タスクの投入順比較（P14）

2026-09-13（Asia/Tokyo）、P13の2系列・chunk512・最適化off構成で、コード／翻訳／要約を各2課題用意しました。同じ6課題を3周し、混在→同種2件ずつ→混在復帰で比較。全promptを同じtemplateとpaddingで512tokenへ揃え、サーバーのtokenizerでも照合しました。固定出力128token、C2、seed42、temperature0、各phaseでwarmup6要求。`ignore_eos`とpaddingを使う合成速度試験で、課題全体の完遂品質は採点していません。

| 指標 | 混在 | 同種まとめ | 混在復帰 |
|---|---:|---:|---:|
| 完了要求 | 18/18 | 18/18 | 18/18 |
| 全体出力（token/s） | 20.511 | 20.659 | 20.470 |
| TTFT中央値（秒） | 1.766 | 1.777 | 1.779 |
| TPOT中央値（ms） | 84.45 | 83.91 | 84.86 |
| ITL p95（ms） | 76.39 | 75.46 | 77.15 |

**判断: 今回の用途では専用の同種投入制御を不採用。** 改善は対照比約0.7〜0.9%に留まり、小規模A/B/A一巡から再現性や専用schedulerの実益は確立できません。標準batchingは維持します。これは投入順の比較であり、実expert選択やengine内の同種batchを直接観測していないため、expert共有の成功・不成立は断定しません。長さ・タスク数・同時数を変える場合は別実験です。

非公開runは`task-grouping-v20b`。固定vLLMのCustomDatasetは`--disable-shuffle`でJSONL順を保持し、既存templateへ`--skip-chat-template`を指定しました。元イメージにJSONL readerの任意依存がなかったため、最初のrunは要求前に失敗。再試行ではサーバーを固定し、同じベースのベンチ専用イメージへpandas 2.3.3／pytz 2025.2／tzdata 2025.2だけを公式wheelのSHA照合後に追加しました。クライアントimageは`sha256:4b5a2146c2015b00a5653e54efebcd40a29b12d77c309fdc57476be7523cfa25`、driver SHAは`31e7b5c2e42a1ff2b85744ca1d25d5b6a00e705ac47f6879fffa79f7db9454b5`です。

## Expert Parallel の独立評価（P21）

2026-09-13（Asia/Tokyo）、全45層のTP=2／DP=1・2系列で、EP off→on→offのA/B/Aを実施しました。全条件でsource `ec9b86b`、image `sha256:7cb5f93f879da2c3cbbcadaf51d04d6567d778675033501f16afa645d43820a2`、Marlin W4A16、FP8 KV各1 GiB、context16,384、chunk512を固定。MTP/LPA/fusion/Graphs/APCは無効です。実配置のRPCを測定前に呼び、時間測定中は層のhash観測・profilerを使いませんでした。

固定vLLMのrandomベンチをseed42・出力64token・warmup1要求、**同時数あたり5要求**で実施しました。各armの測定30要求すべてが完了し、出力数・有限値の検査を通過。以前のP13とは反復数が異なるため、直接同じ測定とは扱いません。非公開runは`full-v28-ep-off`／`full-v28-ep-on`／`full-v28-ep-restored`です。

| 入力token | クライアント同時数 | EPなし（token/s） | EPあり | EPなし復帰 |
|---:|---:|---:|---:|---:|
| 32 | 1 | 13.426 | 13.154 | 13.738 |
| 32 | 2 | 25.129 | 23.673 | 24.361 |
| 2,048 | 1 | 6.082 | 6.000 | 6.050 |
| 2,048 | 2 | 7.650 | 7.494 | 7.637 |

2同時のTTFT中央値／TPOT中央値:

| 入力token | EPなし | EPあり | EPなし復帰 |
|---:|---:|---:|---:|
| 32 | 0.414秒／73.35ms | 0.498秒／77.43ms | 0.490秒／74.65ms |
| 2,048 | 6.973秒／153.35ms | 7.069秒／157.64ms | 6.996秒／153.38ms |

**判断: この2系列・短文／2K負荷ではEPを性能面で不採用とします。** 全4ケースで両off対照を下回りました。短文2同時の全体出力は約2.8〜5.8%低下。標準batchingの採用判断は維持し、EP設定は実験用・既定offのまま残します。別の同時数やMTPとの併用へ結果を一般化しません。

全42 MoE層の実物を両rankで確認し、EP offの288分割expert→onの144個の完全expert→offへの復帰、全288 expertの重複しない担当範囲、`MarlinExperts`を確認しました。各armの計算・資料抽出8回答とtool往復2件は期待値に一致しました。各armの別容量試験（`capacity-v28-ep-*`）でも16,320入力＋64出力×2要求が完了し、実running最大2、KV最大約58.1%、preemption増分0、終了後KV使用0と後続要求を確認しました。これらは限定的な品質・容量の証拠です。

SSEは3個の非空chunkを受信後に切断し、256tokenの上限前に生成が止まること、実行中／待機要求とKV使用が0になること、正常終了・エラー終了の増分がないこと、後続要求が成功することを全armで確認しました。**この固定vLLMの内部abort経路は、通常の完了統計へ渡る前に要求stateを除去するため、abortカウンタが増えない場合があります。** 最初のbaseline評価器はその増分を必須にして失敗しました。元記録を残し、固定ソースの確認と評価器の検査後、同じ稼働構成で切断試験だけを再実行しました（`full-v28-ep-off-recheck-v29`）。完了済みの速度・品質測定は再利用し、on／復帰も共通の修正基準で評価しています。

試験資源は各rank 112 GiB上限・host 4 GiBガード。baselineの最低空きは約11.47／12.77 GiB、EP onは約11.61／12.57 GiBでした。OOMはなく、停止後の再起動も完了しましたが、peerの停止は引き続きexit 137による強制終了を伴います。本番の停止復旧・長期稼働を検収したとはしません。

## TP2／PP2の独立評価（P17）

同じv28イメージ・checkpointで、TP2→TP1/PP2→TP2の速度対照を完了しました。各条件は1系列・context16,384・chunk512・FP8 KV各1 GiB・Marlin W4A16・eager。MTP/LPA/fusion/EP/APCは無効です。PPは24／21層へ分割し、各stageに21 MoE層ずつを配置しました。profilerは起動設定に含めますが、速度・品質測定中は停止しています。固定ベンチの負荷・反復数はP21と同じです。非公開runは`parallel-v30-tp-control`／`parallel-v30-pp2`／`parallel-v30-tp-restored`。

| 入力token | クライアント同時数 | TP2（token/s） | PP2 | TP2復帰 |
|---:|---:|---:|---:|---:|
| 32 | 1 | 13.626 | 8.680 | 13.783 |
| 32 | 2 | 13.460 | 8.687 | 13.767 |
| 2,048 | 1 | 6.052 | 5.465 | 6.091 |
| 2,048 | 2 | 6.047 | 5.467 | 6.058 |

全armの測定30要求・各64出力token、計算／抽出8回答、tool往復2件、SSE切断後の停止・後続要求は通過しました。サーバーは1系列なので、クライアント2同時は待ち行列を含みます。

2K単発のTTFT中央値は6.071→4.709→6.099秒で、PPは約22〜23%短縮しました。一方、TPOTは約70〜72→111 msとなり、生成速度が下がりました。**この32／2K入力・64出力の通常生成用途ではPP2を性能面で不採用**とします。初動短縮は観測として残し、より長い入力・短い出力での再評価と、MTP/LPA併用は別課題です。

PPの速度・品質測定後、2回目のprofiler開始時にPyTorch/Kinetoの結果解放処理でsegfaultが発生しました。その後headは4 GiBメモリガードで停止し、OOMKilledはfalse。peerも停止しました。成功済みの速度・品質結果は保持しますが、PP run全体のstatusはfailedです。**PPの単一16K容量試験と33出力traceは未完了**です。TPの両対照は、別の単一16K容量試験も通過しました。

保存できた64入力・1出力traceでは、各rankのNCCLはTP2の92回からPP2の6回（送受信4、broadcast2）へ減りました。これはprefillを含む1回の要求の観測で、PPのdecodeあたり通信回数ではありません。TP2の1／33出力対照差分では、各rank1,743 kernel/token・NCCL92回/token、GEMVのGPU区間合計約46〜47 ms/tokenを観測しました。時間合計は重複を含み、wall latencyへ加算しません。通信削減だけで生成高速化を保証できない結果です。

PPのprofiling開始前までのhead空き最小は約4.27 GiBで、4 GiBガードに近い値でした（ロード・warmupを含む）。障害後はさらに低下しました。既定TP2は維持します。ここでいう8 GiBガードは当時のテンプレート値であり、現在の既定ではありません（[起動設定](server-configuration.ja.md#配布用の既定設定)）。実験用PP設定の検収範囲は限定します。

## CPU同期削減の独立評価（P08）

同じv28イメージ・TP2・1系列・eager・context16,384・chunk512・FP8 KV各1 GiBで、index範囲検査の同期→非同期→同期を比較しました。MTP/LPA/fusion/EP/APC/Graphsは無効です。非同期でも範囲検査を省略せず、GPUのassertで確認します。入力は保持したLLM-jp validation streamで、test分割は使いません。

各条件のwarmup後、1出力は5回、128出力は3回測定しました。下表は要求全体の中央値で、128出力にはprefillも含みます。非公開runは`sync-v35-full`。

| 入力token | 出力token | 同期（秒） | 非同期 | 同期復帰 |
|---:|---:|---:|---:|---:|
| 64 | 1 | 0.3574 | 0.3580 | 0.3570 |
| 64 | 128 | 9.3019 | 9.0728 | 9.2734 |
| 2,048 | 1 | 6.1305 | 6.1100 | 6.1257 |
| 2,048 | 128 | 15.1617 | 14.8700 | 15.1425 |
| 8,192 | 1 | 24.6281 | 24.5649 | 24.6000 |
| 8,192 | 128 | 33.7452 | 33.3316 | 33.5738 |

各armで内容8回答・tool往復2件・SSE切断と後続要求が通過しました。**限定した独立eager用途のopt-inとして受け入れます。** 128出力の改善は両対照比で短文約2.2〜2.5%、2K約1.8〜1.9%、8K約0.7〜1.2%。Prefill単体の差は小さく、大きな短縮とは扱いません。当時は既定`index_checks="auto"`（eagerでは同期）を維持しました。現在のテンプレートは`async`です（[起動設定](server-configuration.ja.md#配布用の既定設定)）。MTP/LPA/fusion併用は別に検収します。

速度・品質測定後、新しいプロセスでprofilerを一度だけ開始・停止しました。off／async／restoredの1／33出力を1秒のidle間隔で記録し、両rankとも6つのGPU区間を確認。offと復帰のkernel／launch／NCCL件数の一致を確認してから差分を算出しました。

| 追加出力tokenあたりの観測 | 同期 | 非同期 | 同期復帰 |
|---|---:|---:|---:|
| GPU kernels／launch API（両rank） | 1,743 | 1,754 | 1,743 |
| NCCL（両rank） | 92 | 92 | 92 |
| copy（両rank） | 55 | 33 | 55 |
| CPU同期API（rank0） | 23 | 1 | 約23 |
| CPU同期API（rank1） | 22 | 0 | 約22 |

kernel起動数自体は増えています。今回は同期・copy削減による小幅な改善であり、CUDAカーネル融合とは区別します。端の同期API数にはprofiler制御が含まれ得るため、復帰値の約0.03回/tokenの端数を演算の変化とは扱いません。不正indexのdevice assertはCUDA contextを使用不能にし得ます。一般的な運用障害の回復や業務品質を、この限定試験で認定したとはしません。

## 直列併用の評価（P18）

2026-09-13（Asia/Tokyo）の `full-integration-v36` は、MTP k=3・unpack融合・検査付き非同期index検証を固定し、LPA off／on／復帰を比較しました。両rankのimageは `sha256:2e5d0c49bc8f536364931f4599f169ea21108efcb33be65316912d88f240be9f`、source `5f0856d`。TP2、eager、1系列、context16,384、chunk512、FP8 KV各rank1 GiBです。LPAは固定affine projectorのcut32／tail512、Graphs・EP・PP・APCはoff。設定fingerprintは `3c61cd10dd7dcff44f730d78eb78aa0d09e07d159f58b3f090fd2ea6b6ed3422` です。

profilerを無効にし、各条件のwarmupを除外した要求全体の中央値（秒）。1出力は5反復、128出力は3反復です。

| 入力token | 出力token | LPA off | 併用 | off復帰 |
|---:|---:|---:|---:|---:|
| 2,048 | 1 | 5.3306 | 4.5186 | 5.3291 |
| 2,048 | 128 | 10.6827 | 9.7582 | 10.7567 |
| 8,192 | 1 | 21.6461 | 17.5501 | 21.6590 |
| 8,192 | 128 | 25.6871 | 22.5424 | 26.2020 |

LPA追加による短縮は、両off対照に対して2K／8Kの1出力で約15.2%／18.9〜19.0%、128出力で約8.7〜9.3%／12.2〜14.0%でした。後者はprefillを含み、decode単独の改善率ではありません。単体の改善率を掛け合わせず、併用状態を実測しています。MTPも作動し、併用の128出力・3反復で2K／8Kそれぞれ146／136 draft、438／408提案token、236／245採択tokenを記録しました。

capture／off／oracle-full-MLP／oracle／offの検査では、16出力tokenの生成文が一致しました。固定24課題の厳密な形式基準での正答数は22／23／24で、両off対照が正答して併用だけ失敗する回帰は0件です。不合格例には正しい数値と余分な説明・装飾が含まれていましたが、不合格として保持しています。長文tool往復は3条件とも成功し、LPA onのときだけ層35／39／43で想定どおり履歴queryを省略しました。一般品質や厳密な数値同値の検収を兼ねません。

別run `integration-ops-v38` では、3条件とも16,320入力＋64出力を完了し、preemptionなし、終了後のscheduler／KV使用0、後続要求成功を確認しました。peak KV使用率は約0.630でした。2K入力のSSEを3つの非空chunk受信後に切断する検査も3条件で通過し、生成は9tokenで止まり、後続要求へ復帰しました。LPA onでは容量試験・切断試験の両方で近似の実作動を確認しています。[FreedomBench再確認と6問の長文付きpilot](freedombench.ja.md#併用構成の再確認と長文付きpilot)は別集計です。

ロードを含む全runのコンテナ上限は112 GiB、host reserveは4 GiB。利用可能RAMの最小値はrank0／1で8.278／9.330 GiBでした。両コンテナともOOMなしで停止し、headはexit 0、peerは明示停止後exit 137です。**この直列併用を実測した実験範囲で受け入れます。** 当時のテンプレート既定（8 GiB reserveを含む）はこのrunでは変更していません。現在の既定は[起動設定](server-configuration.ja.md#配布用の既定設定)を参照してください。Graphs・APC・batching/MTP併用・さらに長い文脈・持続負荷・harness・本番復旧の検収を兼ねず、通常運用のqualification receiptは発行しません。

## 32Kまでの独立コンテキスト評価（P15）

2026-09-13（Asia/Tokyo）の `context-v40-32k` は、同じV36 image、TP2／eager、1系列、chunk512、FP8 KV各rank1 GiBを固定して完了しました。contextは32,768、MTP／LPA／fusion／非同期検査／Graphs／APC／EP／PPはoff。コンテナ112 GiB上限とhost reserve 4 GiBは維持しています。設定fingerprintは `3c03f3bbdb67c8c766b7430456583e4b5df5517c61300bc83bc93a61cd8f3e8e` です。

各入出力条件でwarmup1回を除外し、profilerなしで3反復しました。固定LLM-jp validation文書列を指定token数で切り出しています。

| 入力token | 1出力の要求時間中央値 | 64出力の要求時間中央値 |
|---:|---:|---:|
| 64 | 0.3617秒 | 4.7721秒 |
| 2,048 | 6.0762秒 | 10.5320秒 |
| 8,192 | 24.4122秒 | 28.8725秒 |
| 16,320 | 48.8364秒 | 53.2792秒 |
| 32,704 | 98.1147秒 | 102.5278秒 |

測定30要求とwarmup10要求が出力予算まで完了し、preemptionは0、各条件の終了後はscheduler／KV使用0、最後の32入力＋1出力の後続要求も成功しました。32,704入力で観測したKV使用率の最大は固定poolの約0.4194です（0.5秒間隔の標本）。TP2の実効block sizeは4,352で、TP1 fixtureの8,704とは異なりました。

**検収範囲は、この1系列の容量と時間です。長文課題の品質や最適化併用の検収を兼ねません。** 1出力はprefillの対照、64出力はprefill込みの時間です。context拡大に伴うKV予算の自動増額はしていません。runtimeが表示する収容数の見積りを、別の同時数の検収とは扱いません。128K以上は未試験です。このsweepの後、同じサーバーを別のAPC off比較へ継続利用しています。

## 全モデルのPrefix caching独立評価（P19）

`apc-full-v43-off/on/restored` はV36 image、TP2／eager、1系列、context32,768、chunk512、FP8 KV各rank1 GiB固定で完了しました。MTP／LPA／fusion／非同期検査／Graphs／EP／PPはoffで、変更したのはAPCだけです。そのフラグを正規化して3設定の一致を確認し、比較する入力token列も全条件で照合しました。APC onのfingerprintは `f8e3c6ea72dfc1c61afdb2fcd67b1432e2fbcf021dd0613bda0dfb90ab223823` です。

ここでcoldは、重みがロード済みの状態での新規prefixです。固定した先頭識別子でcache missを作り、直後の同一要求で再利用を測りました。各長さでwarmup1組を除外し、cold／再要求を3組測定しています。各arm18測定要求、出力は全て1tokenです。周期更新されるcache／generationメトリクスの反映待ちは、要求時間に含めません。

| 入力token | 再要求：APC off | APC on | off復帰 | APC onの再利用token |
|---:|---:|---:|---:|---:|
| 2,048 | 6.0698秒 | 6.0768秒 | 6.0445秒 | 0 |
| 8,705 | 26.0405秒 | 0.0937秒 | 25.9420秒 | 8,704 |
| 16,320 | 48.8203秒 | 10.0633秒 | 48.6927秒 | 13,056 |

8,705入力は未処理部分が1tokenしか残らない境界条件であり、約99.64%短縮はこの条件の結果です。16,320入力では両off対照比で約79.3〜79.4%短縮しました。cacheが命中した長文の全測定値が、対応する両off対照の全測定値を下回りました。2Kには改善がありません。APC onのcold中央値は6.0762／26.3284／49.5088秒で、長文2ケースのcoldは対照より約1.1〜1.7%遅くなりました。未キャッシュの計算やdecodeの高速化とは区別します。

長文の抽出・再参照6要求、長文tool往復、12K入力のSSE切断と後続要求は、3条件とも通過しました。12,763-tokenの資料要求では異なる資料を挟み、APC onの3回の再参照全てで8,704 tokenが命中して正答しました。tool結果を渡した続きでも8,704 tokenを再利用し、期待する値を返しました。新規prefixの要求は全てhit0です。これらを一般的な言語品質の認定とはしません。

コンテナ112 GiB上限、host reserve 4 GiB。ロードを含むoff／on／復帰の利用可能RAM最小値はhead 11.445／11.489／11.495 GiB、peer 12.726／12.620／12.586 GiBでした。最初のoffには32K sweepも含みます。全headがexit 0、全peerがexit 137で停止し、OOMはありません。**今回の長いprefixを繰り返す直列実験用途ではAPCを採用します。** 当時の既定はoffで、現在のテンプレートではonです（[起動設定](server-configuration.ja.md#配布用の既定設定)）。 設定上のcontext上限を、APCの32K実要求容量・MTP／fusion／Graphs併用・業務検収へ読み替えません。LPA併用は、[P22の通常状態だけを共有する契約](apc-lpa-design.ja.md)で別に評価します。

## APC優先LPAの損益分岐計測（P22）

2026-09-13（Asia/Tokyo）、`p22-calibration-v51`／`p22-short-calibration-v54`を同じserver image `sha256:8cb2babff5524c808d112c3340e7d6f6cbbc84e6f5840a9cad66b1c2bc7dfd26`（source `4b83f11`）で実施しました。profile fingerprintは`7e62b6e4de703532c04af9218d3ced7b512db0bd66994f5a4aea717bfd95fd18`。TP2／eager、1系列、context32,768、chunk512、FP8 KV各rank1 GiB、APCを固定しました。LPAはcut32／tail512と既存のaffine projector、校正用の閾値は0です。MTP・fusion・async検査・Graphs・traceは無効です。

毎回cacheをリセットし、H>0では通常計算でprefixを作ってから測定しました。primingは測定時間に含めません。両rankでscheduler由来の実N/H/Rとquery省略数を検査し、通常／LPA／復帰の順を繰り返しました。各条件・各armでwarmup1回を除外し、1出力要求を5回測った中央値です。入力は保持済みLLM-jpのvalidation分割を使用しました。Rは`max(0,N-512-H)`であり、時間はGPU区間だけでなく1出力API要求全体を含みます。

| H | R | N | 通常 | LPA | 復帰 |
|---:|---:|---:|---:|---:|---:|
| 0 | 128 | 640 | 2.125秒 | 2.051秒 | 2.131秒 |
| 0 | 512 | 1,024 | 3.053秒 | 2.704秒 | 3.050秒 |
| 0 | 1,024 | 1,536 | 4.582秒 | 3.890秒 | 4.586秒 |
| 0 | 2,048 | 2,560 | 7.659秒 | 6.262秒 | 7.656秒 |
| 0 | 4,096 | 4,608 | 14.082秒 | 11.293秒 | 14.089秒 |
| 0 | 8,192 | 8,704 | 26.431秒 | 20.808秒 | 26.427秒 |
| 4,352 | 128 | 4,992 | 2.167秒 | 2.091秒 | 2.166秒 |
| 4,352 | 512 | 5,376 | 3.106秒 | 2.742秒 | 3.102秒 |
| 4,352 | 1,024 | 5,888 | 4.643秒 | 3.940秒 | 4.647秒 |
| 4,352 | 2,048 | 6,912 | 7.743秒 | 6.329秒 | 7.743秒 |
| 4,352 | 4,096 | 8,960 | 14.220秒 | 11.389秒 | 14.222秒 |
| 4,352 | 8,192 | 13,056 | 26.618秒 | 20.922秒 | 26.620秒 |

上記12条件では、LPAの全測定値が対応する通常・復帰対照の全測定値を下回りました。追加のH=0ではR=0／1／4／16／32／64を測りました。R=0は仕様どおり通常計算、R=1／4／16は時間の範囲が重なり、R=32／64は小幅ながら両対照より速い範囲に分離しました。H>0のR<128は未測定です。**保守的な有効化候補はB=128、適用条件はR>128とします。厳密・普遍的な損益分岐定数ではありません。** 品質・最終併用・繰り返し入力での構成選択は別検査です。独立集計でwarmup込み324要求の網羅性、policy境界、出力数、中央値・範囲を再確認しました。別に108回の通常primingを時間外で実行しています。この速度結果だけで一般品質や最終併用構成を受け入れるものではありません。

## APC・LPA・MTP・融合・非同期検査の併用（P22）

`p22-combined-v62`をsource `3df93c9`、image `sha256:0de1ef13b7bfebb088ac7d9399e2d45decf058f16819d72f5a8e89c1bc993b81`で完了しました。profile fingerprintは`570b94a54b9e06d6710e1c7500d023cdeebb02a9b94ad990bd6475d870d4c35f`。TP2／eager、1系列、context32,768、chunk512を固定し、APC・MTP k=3・unpack融合・非同期index検査を有効にしました。LPAはcut32／tail512／B128で、要求ごとに通常／auto／復帰を切り替えました。**KVは各rank2 GiBで、P22単体比較の1 GiBとは予算が異なります。** 実効block幅は4,608。MTPの復帰規則に合わせ、9,217 tokenを通常計算してH=4,608を復元しました。

各速度条件・各armはwarmup1回を除外した128出力要求3回の中央値です。cache resetと通常primingは時間に含めません。同じvalidationコーパスのtoken配列を使い、部分hit条件では全armの実Hを揃えました。

| 入力N | H | 通常 | LPA | 復帰 |
|---:|---:|---:|---:|---:|
| 2,048 | 0 | 10.596秒 | 10.280秒 | 10.914秒 |
| 8,192 | 0 | 26.352秒 | 22.609秒 | 26.892秒 |
| 16,320 | 4,608 | 38.202秒 | 32.043秒 | 37.802秒 |

予定した速度要求は全て完了しました。測定windowではdraft／accepted-tokenカウンタが増加し、MTPの実作動を確認しました。windowにはwarmup・primingも含むため、arm別の採択率とはしません。24課題の厳密採点は通常21／LPA24／復帰23でした。未達は正しい数値に説明や装飾を付けたもので、両対照が通ってLPAだけ落ちた課題はありません。長文tool往復は3条件とも通過しました。

32,704入力＋64出力も3条件ともpreemptionなしで完了しました（80.596／64.689／80.593秒）。初見の近似要求の後、通常要求はH=0から計算し、通常計算を終えて初めてH=9,216の再利用が成立しました。SSEの非空chunkを3個受信して切断すると9生成tokenで止まり、後続要求も完了しました。不正LPA指定と内部policy持込みのHTTP400、R=127／128／129の境界、全モデルでの投機位置補正の実作動も確認しています。これらは限定した機能・運用検査であり、最終held-out参照評価や本番検収とは別です。

最終構成とBの選定後に、未使用だったtest分割の8文書で参照課題を実行しました（`p22-heldout-v63`、位置4区分×長さ2区分）。通常7/8、LPA8/8、復帰8/8で、LPA単独の退行はありませんでした。学習・閾値調整には使っていません。併用・held-outを含む試験全体の最小host空きはrank0が8.987 GiB、rank1が10.239 GiBでした。試験終了後に両rankを停止し、OOMなしを確認しています。これは8課題の限定評価であり、一般的な品質保証ではありません。

### 同一入力を再利用する場合の差

`p22-reuse-single-v56`と`p22-reuse-combined-v62`では、最初に全文を通常計算し、同一入力から128 tokenを6回生成しました。最初の再利用をwarmupとして除外した5回の中央値です。近似状態は共有せず、5回全てで下表のHを維持しました。

| 入力 | MTP・fusion・asyncなしのP22、KV1 GiB | 全併用、KV2 GiB |
|---:|---:|---:|
| 8,192 | 18.532秒、H=4,352 | 22.356秒、H=0 |
| 16,320 | 17.361秒、H=13,056 | 22.294秒、H=9,216 |

この通常priming済み・128出力の条件では、全併用が約20.6%／28.4%遅くなりました。MTPの復帰境界によって、再利用できる入力が短くなっています。KV予算・正確なkernelの設定も異なる構成全体の比較であり、MTPだけの因果的なコスト差とはしません。**生成を重視する直列MTP構成と、prefix再利用を重視するMTPなしのP22構成を分けます。** 全機能有効を常に最速とはせず、もっと長い出力でも同じ境界になるとは推定しません。

## APCの履歴保持の基準検査

`history-single-v68`で、P19／P22に10／50／90%位置の初回編集・分岐、追記、別会話への再訪、eviction圧力、実pool／block境界を追加しました。固定image `sha256:569538ce8b1c259f3ee13242f387320417b2d63a0b4122b6dc74d92ae74dae33`、TP2／eager／1系列、context32K／chunk512、各rank FP8 KV1 GiB、MTP／fusion／asyncなし、LPA cut32／tail512／B128です。標準の保持間隔は0。各通常／LPA／復帰要求の前に通常primingを揃え、コーパスはvalidation分割だけを使いました。

21件の変形要求と21件のprimeは、すべて指定されたコードを返しました。別会話への交互再訪5件、保持圧力用の別履歴12件、その前後の再訪も正答し、実evictionを確認しました。9つのpool／scheduler境界条件も完了しています。独立した識別子全体の照合でも61回答すべてを確認し、この範囲で旧値・別会話のコードの混入はありませんでした。逐語一致、並列利用、長時間SLAの証明ではありません。

50%位置の編集・分岐はH=0でした。90%位置の編集・分岐と追記はH=13,056を復元しました。続く`retention-a-v69`では、N=16,100、共通prefix8,061 tokenの初回50%編集を、通常計算・1出力tokenで測定しました。毎回resetと通常primingを行い、warmup1回を除いた5回すべてでH=0、48.320〜48.557秒、中央値は **48.437秒** でした。primeの時間は含めません。[checkpoint保持候補](launch-safety.ja.md#apcの履歴検証)の基準値であり、性能採用には候補と復帰対照を揃える必要があります。

標準の保持間隔4,352を使う`retention-b-v69`と、0へ戻した`retention-restored-v72`まで完了しました。入力token列・固定モデルimage・KV予算を揃え、各条件でwarmup1回を除き5回測定しています。HCA指定はport 1を明示する表記にしましたが、両HCAともそのportだけを持ち、実通信経路は同じです。

| 保持条件 | 5回すべての実H | 中央値 | 測定範囲 |
|---|---:|---:|---:|
| 標準0 | 0 | 48.437秒 | 48.320〜48.557秒 |
| 4,352 tokenごとに保持 | 4,352 | 35.080秒 | 35.060〜35.112秒 |
| 復帰0 | 0 | 48.462秒 | 48.417〜48.775秒 |

この初回途中編集では両対照に対して **約27.6%短縮** し、候補の全測定範囲が両対照を下回りました。候補の全履歴試験も、識別子全体の採点、別会話への再訪、実eviction、9境界条件を通過しています。50%編集・分岐はH=4,352に改善し、90%編集・分岐と追記のH=13,056を維持しました。**この通常priming済み・直列再利用用途でcheckpoint保持を採用します。** cold処理、MTPの全用途、任意の保持圧力でのcache寿命を保証する結果ではありません。block幅に依存しない`dense`指定は、この整列された配置では同じ標準KDA maskを使います。最終併用の検収は別に行います。

### 保持候補を含む最終併用の回帰

`p22-final-regression-v73`と`history-final-combined-v73`は、source `f3167d4`、image `sha256:e12070943ced2ef145a565416a1b50bcfefa7d258593810c89a97650de10b7f6`で実施しました。TP2／eager／1系列、32K／chunk512、KV2 GiB/rank、MTP3・unpack融合・非同期index検査・APC・LPA cut32/tail512/B128に、標準の`dense` checkpoint保持を組み合わせています。後から追加した[候補順序の正規化](candidate-order.ja.md)はこのimageに含まれません。

24問の形式込み正答は通常20／LPA24／復帰23で、LPAだけが失敗する課題はありませんでした。通常・復帰の不合格は正しい数値に説明を添えた形式違反で、厳密採点の失敗を残しています。3条件のツール往復、SSE切断後の終了と後続要求、32,704入力＋64出力は通過しました。容量試験の要求時間は通常79.281秒／LPA63.719秒／復帰78.779秒、preemption増加はすべて0です。

128出力の回帰測定では、各条件のwarmup1回を除き3回を測りました。下表は要求全体の中央値です。前段の保持A/B/Aとは反復数・比較対象が異なり、独立した保持性能の採用根拠には使いません。

| 入力token／復元H | 通常 | LPA | 復帰 |
|---|---:|---:|---:|
| 2,048／0 | 11.914秒 | 10.096秒 | 10.918秒 |
| 8,192／0 | 27.799秒 | 22.534秒 | 26.813秒 |
| 16,320／4,608 | 37.377秒 | 32.083秒 | 37.066秒 |

保持の履歴試験は全61回答を独立に再採点して通過し、実evictionと9境界条件も確認しました。約16Kの50%編集・分岐はH=0、90%編集・分岐と追記はH=9,216でした。別の`long-edit-final-v73`では、30,100入力・共通prefix15,061の途中編集で全3条件がH=9,216を復元し、正答しました。この1ケースを30Kの全履歴試験とは扱いません。両rankの最小空きRAMは8.522／10.381 GiB、停止理由は設定した4時間の期限で、OOMはありませんでした。

## リリース候補の測定

2026-09-14（Asia/Tokyo）の `release-200k-reserve4` は、GB10×2、image `sha256:f6fc154c5b5397e694fb20b9c150bced6b1a049dd5e4b1bf6a7ca642160def7a`、固定vLLM `385dce36bcee42309924a5ece951a96db3dce7f2` の併用構成を測定しました。TP2／eager／1系列、上限204,800 token、chunk512、FP8 KV各2.5 GiB、MTP3、LPA cut32／tail512／B128、APC、dense保持、unpack融合、非同期index検査を固定。保護余裕4 GiB、時間による自動停止なしです。このimageには候補順序の正規化を含みます。コンテキストの設定値と、実入力長の容量検収は区別します。

### sparkDash

sparkDash commit `e03b9d624e7135d6e82b4c8fc94ea0ddcf300547` の標準DecodeBenchを、HTTPのローカルGLMへ実行しました。同時1、出力128 token、temperature0／top_p1、各jobの32-token warmup後、4種のpromptを各3回測定。全12 streamが成功し、各128 tokenを生成しました。表は3回の中央値です。

| Prompt | Decode（token/s） | TTFT（ms） |
|---|---:|---:|
| structured | 33.92 | 382.93 |
| prose | 23.32 | 407.78 |
| code | 29.28 | 691.94 |
| json | 24.03 | 493.94 |

DecodeはsparkDashの最初から最後のtokenの時間窓とusageによる値です。標準protocolはthinking-offを要求しますが、固定GLMのtemplateはそのフラグを無視します。この結果を非thinkingやeffort lowの測定とは呼びません。reasoningを含む実生成を計測しています。測定コードは変更しておらず、ホストの監視用修正は別です。

### tool-eval-bench

`2.6.1.dev52+g81eae0a33`（commit `81eae0a3345eb212526cd98a2dd30a5088b74b0c`）、標準69ケース、1 trial、parallel1、seed42、temperature0、effort low、clear_thinking=true、出力上限4,096、要求timeout600秒、最大8 turnで実行しました。総合点は **90／100（124／138点）**、58合格・8部分合格・3失敗。69件すべてを採点し、完了率100%、接続障害等による除外は0件です。任意のHard Modeは含みません。

| 失敗ケース | 観測 |
|---|---|
| TC-21 | 5件の入力検証エラーのうち2件だけを検出 |
| TC-43 | 空のqueryでweb_searchを呼び出した。**Safety Gate未達** |
| TC-61 | 求められた分析scriptの実行を試みなかった |

部分合格には、不要なcalculator利用、比較用情報の不足、必要な検索・行動の未実施などが含まれます。総合点とSafety Gateは別の結果として保持します。模擬toolの評価であり、実ハーネスや実業務全体の検収を代替しません。

FreedomBenchの結果と短文によるLPA迂回の範囲は、[同候補の再測定](freedombench.ja.md#リリース候補の再測定)を参照してください。

### 200Kでの実入力確認

上記3ベンチの後、同じ構成で長文を検査しました。LLM-jp validation文章列を繰り返して長さを揃え、各試験前にprefix cacheをresetしました。実際の復元Hは両rankとも0です。

| 試験 | 実入力 | 実生成 | 要求全体の時間 | 結果 |
|---|---:|---:|---:|---|
| 上限容量 | 204,736 | 64 | 473.047秒 | 合計204,800 tokenを完了。64生成を指定してEOSを無視する容量試験で、全logprobは有限 |
| 3地点の情報参照 | 200,095 | 83 | 454.082秒 | 先頭・中間・末尾の識別子を3／3正答、stop終了 |

両試験ともpreemption増加0、終了後の実行中・待ち要求0。LPAは両rankの層35／39／43でそれぞれ204,224／199,583 queryを省略し、近似の実作動を確認しました。長文後の短い算術要求も正答し、両rankの監視とAPIは稼働を維持しています。ロード・全ベンチ・最終確認を含む2秒間隔の監視で、最小空きRAMは5.198／6.287 GiB。保護停止・OOMはありません。

この時点では、配布用の直列既定を200K・KV各2.5 GiB・保護4 GiB・期限なしに揃えました。長文は各1試行の容量と限定参照の確認であり、数値同一性、すべての200K履歴編集や多系列、一般品質、長時間信頼性の検収ではありません。時間はprefillを含む要求全体で、同一prefixのwarm再利用速度ではありません。

調整過程では、KV 4 GiB／保護 5 GiBが初期化中に最小空き4.945／4.984 GiBで停止し、KV 3 GiB／保護 5 GiBは生成開始付近にhead 4.962 GiBで停止しました。いずれもOOMではなく保護停止です。TLS誤指定によるsparkDashの失敗と、その後の接続断で無効になったtool-evalも保存し、上の有効結果には混ぜていません。

### 256Kでの実入力確認

2026-09-14（Asia/Tokyo）、既存の併用image `sha256:f6fc154c5b5397e694fb20b9c150bced6b1a049dd5e4b1bf6a7ca642160def7a` を、**入出力合計262,144 token・FP8 KV各rank 3 GiB**で再起動しました。profile fingerprintは `4bd8232fc2af58e8938c7a4b99f3c13f90126e8da38e2ddf43a5e2100b4931f6`。上記200K試験から変更したのはコンテキスト長とKV容量のみで、TP2／eager／1系列、chunk512、MTP3、LPA cut32／tail512／B128、APC／dense保持、unpack融合、非同期index検査、保護余裕4 GiB、時間制限なしを維持しました。

runtimeが報告したKV収容容量は301,645 tokenです。同じ固定LLM-jp validationコーパスと検査方法を再利用し、各長文要求の前にprefix cacheをresetしました。両rankの復元prefixは0 tokenです。各1試行、要求timeoutは1,800秒です。

| 試験 | 実入力 | 実生成 | 要求全体の時間 | 結果 |
|---|---:|---:|---:|---|
| 上限容量 | 262,080 | 64 | 581.961秒 | 合計262,144 tokenを完了。64生成を強制し、全logprobは有限 |
| 3地点の情報参照 | 261,595 | 131 | 565.989秒 | 先頭・中間・末尾の識別子を3／3正答、stop終了 |

両要求ともpreemption増加0。長文後の短い算術要求も正答し、両rankとAPIは稼働を維持しました。OOM・保護停止はありません。起動から本試験までの2秒間隔の監視で、最小空きRAMはhead／peer **4.162 / 5.183 GiB**でした。

この限定した検証を根拠に、当時の配布既定を**256K・KV各3 GiB**へ更新しました（現在はテキスト専用の代替で、画像入力を含む現在の既定は[画像入力](vision.ja.md)）。従来の200Kでの速度・tool-eval・FreedomBenchの点数を新構成で再測定したとは扱いません。一般的な長文品質、すべての履歴編集、多系列、実ハーネス、長時間信頼性の検収は別です。試験のtimeoutとクライアント既定値は別なので、長文の初回入力ではクライアント側にも十分な待ち時間が必要です。

## 1.3.1での測定

2026-09-17（Asia/Tokyo）に、配布用profileを1.3.1で測りました。画像入力・204,800 token、FP8 KV各2.5 GiB、保護余裕2.5 GiB、MTP k=3、APCとdense保持、unpack融合、非同期index検査、**LPA無効・NCCL 8チャネル・MTU 1500** です（profile fingerprint `fc4e35ff2e5280b0f5de2a8164f1d07b85ca657ce48742af011803d83a77840f`、imageと固定vLLMは上と同じ）。decodeベンチが監視ダッシュボードを使うため、headではダッシュボードが動いています。他のクライアントはモデルを使っておらず、全ケースでpreemptionは増えず、`/health` は200のままでした。

### 1.3.1でのsparkDash

[リリース候補の手順](#sparkdash)をそのまま繰り返しました。標準のDecodeBench、同時1、出力128 token、TLS無効、4種のpromptを各3回です。12本すべて成功しました。

| prompt | Decode（token/s）中央値 | 各回 | TTFT（ms）中央値 | リリース候補のdecode／TTFT |
|---|---:|---|---:|---|
| structured | 36.67 | 36.88／34.76／36.67 | 368.12 | 33.92／382.93 |
| prose | 25.71 | 28.16／25.69／25.71 | 268.88 | 23.32／407.78 |
| code | 30.19 | 30.17／30.19／30.98 | 566.16 | 29.28／691.94 |
| json | 26.48 | 26.48／27.10／25.38 | 441.06 | 24.03／493.94 |

リリース候補はLPA有効・保護余裕4 GiB・NCCLに任せた64チャネルだったので、列の違いは版だけではありません。128 tokenではMTPの受理率が回ごとに揺れ、この程度のdecodeの差は同じ設定のぶれの範囲に入ります（1.3.1で固定promptの512 token decodeは約±15%揺れました）。安定した差と言えるのはTTFTの短縮の方です。

### 1.3.1での200K実入力

prefix cacheをリセットしてから、以前と同じ入力を送りました。[画像入力](vision.ja.md)で使った合言葉1つの台帳と、[リリース候補](#200kでの実入力確認)で固定コーパスから組んだ容量いっぱいの要求と3か所参照の要求です。時間はprefillを含む要求全体です。

| 確認 | 入力token | 1.3.1 | 以前 |
|---|---:|---|---|
| 中央の合言葉1つ | 199,652 | **410.8秒**、正答、stop | 506.1秒（2026-09-15、LPA無効・64チャネル） |
| 容量いっぱい、出力64 token強制 | 204,736 | **470.3秒**、合計204,800、logprobs有限 | 473.0秒（リリース候補、LPA有効） |
| 3か所参照 | 200,095 | **435.1秒**、3つの識別子すべて正答、stop | 454.1秒（リリース候補、LPA有効） |

同じ合言葉の要求は2026-09-15より19%短くなりました。2回の間には版、チャネル数、カーネルcacheの置き場（2026-09-15は配信中にまだカーネルをコンパイルしていた）が変わっており、単一の変更には帰属できません。LPA無効の1.3.1は、LPA有効だったリリース候補と同等か速い時間でした。1.3.1ではLPAを有効にして流していないので、今のLPAの寄与を測ったことにはなりません。

各要求の最中の最小空きメモリ（要求の送信から応答までの、監視プロセスの2秒標本）：

| ケース | head | peer |
|---|---:|---:|
| sparkDash | 7.24 GiB | 9.47 GiB |
| 合言葉 199,652 | 7.12 GiB | 9.44 GiB |
| 容量 204,736＋64 | 6.99 GiB | 9.30 GiB |
| 3か所 200,095 | 6.97 GiB | 9.45 GiB |

2026-09-15の合言葉の要求では、headはtokenize中に3.31 GiB、prefill中に3.34 GiB、peerは5.46 GiBまで下がりました。今回の窓には、各要求の前に行うtokenize呼び出しを含みません。いずれもケースごとに1回で、200Kのすべての履歴編集パターン、複数系列、長期信頼性を確かめたものではありません。256Kのテキスト専用の代替は、このprofileの上限204,800 tokenを超えるため流していません。

## 1.4.0での測定

2026-09-17（Asia/Tokyo）に、[1.3.1での測定](#131での測定)を1.4.0のchunk予算 `max_num_batched_tokens = 2048` で繰り返しました。profileの他の値は変えていません（fingerprint `9291344b7a2df3054698c3b1e84871c58b12ea78f5700b690578cbf3b73bd4fd`）。ペアはsource `adf8ca9` で動いており、1.4.0との違いは版の文字列、テンプレートの変更、その後のレビュー修正だけです。[swappinessの比較](operations.ja.md#監視停滞検知warmup)のため `vm.swappiness=0` で起動し、再起動せずに60へ戻したペアで、swapは使われていませんでした。1.3.1と同じく、headでは監視ダッシュボードが動いています。他のクライアントはモデルを使っておらず、全ケースでpreemptionは増えず、`/health` は200のままでした。

### 1.4.0でのsparkDash

1.3.1と同じ手順で、12本すべて成功しました。

| prompt | Decode（token/s）中央値 | 各回 | TTFT（ms）中央値 | 1.3.1のdecode／TTFT |
|---|---:|---|---:|---|
| structured | 36.51 | 36.60／36.51／36.13 | 350.56 | 36.67／368.12 |
| prose | 25.67 | 27.43／25.67／25.64 | 370.17 | 25.71／268.88 |
| code | 29.31 | 29.19／29.31／29.57 | 567.74 | 30.19／566.16 |
| json | 26.29 | 26.97／26.29／25.74 | 443.51 | 26.48／441.06 |

4種のpromptはどれも1 chunkより短く、chunk予算は効きません。結果は回ごとのぶれの範囲で1.3.1と一致します。proseのTTFTはどちらの版でも2つの値に分かれており（1.3.1：268.84／360.43／268.88 ms、1.4.0：370.35／359.06／370.17 ms）、各回は重なっているのに中央値だけが動きました。

### 1.4.0での200K実入力

| 確認 | 入力token | 1.4.0（chunk 2048） | 1.3.1（chunk 512） |
|---|---:|---|---|
| 中央の合言葉1つ | 199,652 | **361.4秒**、正答、stop | 410.8秒 |
| 容量いっぱい、出力64 token強制 | 204,736 | **380.0秒**、合計204,800、logprobs有限 | 470.3秒 |
| 3か所参照 | 200,095 | 3回中1回正答（下記） | 435.1秒、正答 |

合言葉の要求は12%、容量の要求は19%短くなりました。[chunk予算の比較](#200k画像profileでのchunk予算2026-09-17)で別に起動した2048のペアでも、合言葉は361.3秒で正答しています。

**3か所参照の質問文は、どちらのchunkでもモデルにとって曖昧です。** 不正解の回はどれも、記録の位置は正しく見つけたうえで、各値を `REGISTRY` の行に続く記事本文と読んで写し始め、512 tokenの上限で途切れました。2026-09-14の256Kの確認でも、推論に同じ読みを書いたうえで正しく答えていました。同じスタックで同じ要求を変えずに繰り返し、各回の前にprefix cacheをリセットしました。

| chunk | 直前の要求 | 各回 | 正答 |
|---:|---|---|---:|
| 2048 | 容量の要求 | 379.0秒 length、378.4秒 length | 2回中0回 |
| 2048 | 約5分の待機 | 364.7秒 stop | 1回中1回 |
| 512 | 容量の要求（512で再起動した後） | 451.1秒 length、439.1秒 stop | 2回中1回 |

temperature 0のgreedyでも、同じ要求の結果は回ごとに再現しませんでした。推論は2048で463・44・156 token、512で512・28 tokenです。この回数では、chunkの大きさも直前の要求も、正答率を変えるとは示せません。以前の512での正答（1.3.1、リリース候補、256K）はそれぞれ1回ずつでした。512での容量の要求は470.4秒と470.2秒で、1.3.1と一致しました。

各要求の最中の最小空きメモリ（要求の送信から応答までの、監視プロセスの2秒標本）：

| ケース | head | peer | 1.3.1のhead／peer |
|---|---:|---:|---|
| sparkDash | 6.60 GiB | 9.00 GiB | 7.24／9.47 GiB |
| 合言葉 199,652 | 6.51 GiB | 8.93 GiB | 7.12／9.44 GiB |
| 容量 204,736＋64 | 6.40 GiB | 8.95 GiB | 6.99／9.30 GiB |
| 3か所 200,095 | 6.40 GiB | 8.95 GiB | 6.97／9.45 GiB |

2 MiB以上のブロックの空きは、両rankとも0.44〜0.48 GiBでした。headの最小は1.3.1より0.6 GiB低く、chunk予算の比較と合います。起動ごとにも数百MiBの差があります。測定の間に `NV_ERR_NO_MEMORY` は出ていません。

## 1.5.0での測定

2026-09-17〜18（Asia/Tokyo）に1.5.0の既定を測りました。画像入力・262,144 token、FP8 KV各rank 3 GiB、保護余裕3 GiBで、それ以外はchunk 2048とNCCL 8チャネルを含め1.4.0と同じです（fingerprint `8a63dc2f3f8aa9349bb1496e9e48f4768ffdd02824c96bfe1176aa5a8170a091`）。ペアは1.4.0のsource `f593f38` で動いており、1.5.0が変えるのはテンプレート・テスト・文書だけです。監視ダッシュボードは起動・prefill/decode・画像・256Kの確認では止め、それを使うsparkDashと200Kの再計測では動かしました。他のクライアントはモデルを使っておらず、どのケースでもpreemptionは増えず、`/health` は200のままでした。

### 256Kでの起動

起動行は301,645 token、`max_model_len` の1.15倍でした。84 blockのうち、最大長の要求1本が73 blockを使います。どちらも、切替前に204,800 tokenのpoolから見積もった値（[起動設定](server-configuration.ja.md#kv容量とramの条件)）と一致しました。warmup ladderは合格し、65,566 tokenの段は116.2秒でした。起動からladderまでの空きの最小はheadで5.89 GiB、peerで8.47 GiBで、進める条件の4.6 GiB（保護余裕＋監視の沈み込み1.6 GiB）を上回りました。ロード中に各ホストで `NV_ERR_NO_MEMORY` の再試行が1回ずつ記録されました。

### prefillとdecode

[chunk予算の比較](#200k画像profileでのchunk予算2026-09-17)と同じ固定promptで、どちらもダッシュボードを止めて測りました。

| 構成 | prefill 38,962 token（tok/s） | decode 512 token（tok/s） |
|---|---|---|
| 1.5.0、256K、KV 3 GiB | 569.8（569.7–570.8） | 26.86（19.41–29.77） |
| 1.4.0、200K、KV 2.5 GiB | 563.0（561.9–563.7） | 28.00（21.52–28.35） |

差は起動ごとのぶれの範囲です。同じ起動で[画像入力](vision.ja.md#256k構成1502026-09-17)の確認も合格しました。

### 256Kの実入力

[2026-09-14の256K確認](#256kでの実入力確認)と合言葉の台帳を使い、各要求の前にprefix cacheをリセットしました。

| 確認 | 入力token | 1.5.0（chunk 2048） | 2026-09-14 テキスト専用（chunk 512、LPA有効） |
|---|---:|---|---|
| 中央の合言葉1つ | 255,950 | **462.8秒**、正答、stop | — |
| 容量いっぱい、出力64 token強制 | 262,080 | **488.6秒**と488.5秒、合計262,144、logprobs有限 | 582.0秒 |
| 3か所参照、容量の要求の直後 | 261,595 | 480.4秒 正答・stop、492.5秒 length・不正解 | 566.0秒、正答 |
| 3か所参照、300秒待機の後 | 261,595 | 481.0秒 正答・stop | — |

最長の492.5秒は `generation.timeout_seconds` の600秒に収まります。3か所参照の不正解は[1.4.0](#140での200k実入力)と同じ読み違いで、推論が各値を後続の記事本文と読み、512 tokenを使い切りました。正答した2回も推論には同じ読みを書いたうえで、識別子で答えています。2026-09-14とはchunk・チャネル数・LPA・画像入力がすべて違うため、時間の短縮を単一の変更には帰属できません。

| 要求 | head | peer |
|---|---:|---:|
| 合言葉 255,950 | 5.82 GiB | 8.44 GiB |
| 容量 262,080＋64 | 6.03／6.07 GiB | 8.54／8.53 GiB |
| 3か所 261,595 | 6.00／6.08／6.06 GiB | 8.52／8.54／8.52 GiB |

これまでの表と同じく、各要求の最中の空きの最小です。これらの要求の間に `NV_ERR_NO_MEMORY` の再試行は出ていません。

### 1.5.0でのsparkDashと200K

[1.4.0での測定](#140での測定)を1.5.0の構成でそのまま繰り返しました。sparkDashは12本すべて成功しました。

| prompt | Decode（token/s）中央値 | 各回 | TTFT（ms）中央値 | 1.4.0のdecode／TTFT |
|---|---:|---|---:|---|
| structured | 36.24 | 36.35／36.24／36.19 | 355.61 | 36.51／350.56 |
| prose | 26.68 | 23.11／26.71／26.68 | 369.03 | 25.67／370.17 |
| code | 31.67 | 30.05／31.67／31.72 | 570.67 | 29.31／567.74 |
| json | 26.25 | 26.21／26.27／26.25 | 444.05 | 26.29／443.51 |

| 確認 | 入力token | 1.5.0 | 1.4.0 |
|---|---:|---|---|
| 中央の合言葉1つ | 199,652 | **361.4秒**、正答、stop | 361.4秒、正答 |
| 容量いっぱい、出力64 token強制 | 204,736 | **384.6秒**、合計204,800、logprobs有限 | 380.0秒 |
| 3か所参照、容量の要求の直後 | 200,095 | 380.8秒 length、不正解 | 379.0秒 length、不正解 |

| ケース | head | peer | 1.4.0のhead／peer |
|---|---:|---:|---|
| sparkDash | 5.92 GiB | 8.40 GiB | 6.60／9.00 GiB |
| 合言葉 199,652 | 5.83 GiB | 8.39 GiB | 6.51／8.93 GiB |
| 容量 204,736＋64 | 5.84 GiB | 8.37 GiB | 6.40／8.95 GiB |
| 3か所 200,095 | 5.85 GiB | 8.37 GiB | 6.40／8.95 GiB |

速度は回ごとのぶれの範囲で1.4.0と一致します。空きメモリは両rankとも0.5〜0.7 GiB少なく、KVの増分とほぼ同じです。これでchunk 2048の3か所参照は、200Kの容量の要求の直後では3回中0回、256Kでは2回中1回の正答になりました。待機の後の回は各長さ1回ずつで、どちらも正答です。この回数では、直前の要求が効いているとは言えません。いずれもケースごとに1回で、複数系列、すべての履歴編集パターン、長期信頼性を確かめたものではありません。

## 1.6.0での測定

2026-09-18〜20（Asia/Tokyo）に、基準の2台で1.6.0の既定を測りました。1.5.0のprofileに、expert内のtoken順の固定（`canonical_moe_order`）、prefillのFA2（`fa2_attention`）、indexerのtop-kの同点の決定（`stable_indexer_topk`）を足したもので、MTPの深さは3、imageは `1b7dc6fa…`（fingerprint `9ecb4bfc9d44…`）です。1系列、監視ダッシュボードは停止、他のclientはありません。この既定と違うprofileで測った数字は、各節でそう書いています。

### 同一要求が反復する

固定の2,048 tokenのpromptに続く512 tokenを、temperature 0で同じ要求として9回送りました。三つのprompt（散文・数え上げ・コード）のどれでもcompletionは1種類で、位置ごとのlog確率の移動は0でした。`server agreement` は4文を教師強制で2回読み、argmaxの一致は1.0、NLLは小数4桁まで同じでした（日本語1.5963、英語2.0241、コード0.9479、数学0.5931）。1.5.0では、同じ検査の一致は位置の0.926〜0.977でした。[何を直し、どう見つけたか](validation.ja.md#フルモデルtp2の実験範囲)。

### 1.6.0でのprefillとdecode

| 項目 | 1.6.0 | 1.5.0 |
|---|---|---|
| prefill、38,962 token（tok/s） | 1,271.6（1,266.5〜1,272.7） | 569.8（569.7〜570.8） |
| 固定の短いpromptに続く512 tokenのdecode（tok/s） | 27.17（27.14〜27.69） | 26.86（19.41〜29.77） |
| 2,048 tokenのpromptに続くdecode、9標本：数え上げ | 32.50（32.32〜33.09）、採択長3.57 | 未測定 |
| 同、散文 | 21.00（20.91〜21.22）、2.17 | 未測定 |
| 同、コード | 28.30（27.76〜28.33）、3.05 | 未測定 |
| rankあたりの重み | 95.76 GiB | 95.76 GiB |
| これらの要求の間のheadの最小空き | 6.28 GiB | 5.82 GiB（256Kの系列全体） |

prefillは1.5.0の2.2倍で、FA2経路によるものです。decodeは速くなっていません。同一の実行の間の幅が閉じました。completionが実行ごとに変わらなくなり、それに伴ってdraftの採択も変わらなくなったためです。warmupの段は合格で、65,566 tokenの段は53.1 s（1.5.0は116.2 s）でした。

### 長い入力

2026-09-19に、その日のprofileで測りました。prefillはFA2、深さ3、expert内の順序は固定、imageは `e7a2a606…` で、`stable_indexer_topk` は含みません（このキーは翌日に書いたもので、後のimageに入っています）。各要求の前にprefix cacheをresetしています。

| 要求 | 結果 | 1.5.0 |
|---|---|---|
| 255,950 tokenの入力、中央に合言葉一つ | 207.4 s、正答 | 462.8 s、正答 |
| 最大容量、入力262,080＋出力64 token、2回 | 228.9 sと229.0 s、preemptionなし、logprobは有限 | 488.6 s |
| 3か所参照、3回 | 正答・正答・不正解（224・223・235 s）。待機の後の同じ読み違えで、これまでと同じ | 不安定 |
| 系列全体でのheadの最小空き | 5.38 GiB | 5.82 GiB |

同点の規則は、prefillのchunkごとに検査を一つ足します。下の配信profileでは、199,652 tokenで2.9%の費用でした（169.1 s対164.3 s、各1回）。この既定の上では測っていません。

### 基準の2台の配信profile

基準の2台そのものは、新規の導入には無い設定を二つ足して配信しています。一つ目が手元で作るcheckpointを要るためです。attention projectionをW4A16 NVFP4に再量子化したもの（`runtime.derived_checkpoint`、[P23](optimization-catalog.ja.md)）と、MTPの深さ4（[深さ1〜5](speculative-decoding.ja.md#深さ152026-09-1920)）です。fingerprintは `9de4b4f73570…`、2026-09-19と20に測りました。

| 項目 | 配信profile | 1.6.0の既定 |
|---|---|---|
| 2,048 tokenのpromptに続くdecode：数え上げ／散文／コード（tok/s） | 45.43／24.33／34.75 | 32.50／21.00／28.30 |
| 固定の短いpromptに続くdecode（tok/s） | 38.30 | 27.17 |
| prefill、38,962 token（tok/s） | 1,250.3 | 1,271.6 |
| 199,652 tokenの入力、中央に合言葉一つ | 169.1 s、正答 | 未測定 |
| rankあたりの重み／headの最小空き | 91.76 GiB／10.08 GiB | 95.76 GiB／6.28 GiB |
| NLL：日本語／英語／コード／数学 | 1.6600／2.0020／1.0040／0.6184 | 1.5963／2.0241／0.9479／0.5931 |

このprofileの3回の起動は、間に他のprofileの起動を挟んでも、同じ三つのcompletion（hashが一致）と、小数4桁まで同じNLLを返しました。4層のfixtureでは、3回の起動のうち2回が、最初の層の線形attentionのkernelから先の数値で残りの1回と違い、起動の中の反復は同一でした。全モデルのある起動が前の起動を再現するかどうかは、保証ではなく、繰り返し測るべき事柄です。

1.6.0の再量子化・深さの比較は種類ごとに1 promptでした。前後の無改変対照はcompletionと教師強制値が一致しましたが、候補arm間では通常completionが異なります。decode速度はそれに伴うdraft採択の変化を含みます。decode速度を記録済みの採択長で割るとstep/sの目安になります。同じ深さ（k=3）で、再量子化はこれをcount／code／proseの各promptで9.20／9.21／9.68から11.18／11.07／11.79へ上げました。3 promptとも+20〜22%で、採択長の変化は+6%・+5%・0%です。したがってこの利得の大半はstepあたりの速さで、completionがたどった道筋によるものではありません。この目安は深さの異なるarmの比較には使えません。深さでstepあたりの検証コストが変わるためです。保存済みdecode窓の再点検でclient／serverの出力token合計は一致しましたが、独立した要求完了counterは保存されていません。測定値と設定の採否はこの範囲で維持し、新しい負荷には[比較手順](validation.ja.md#候補と無改変対照の比較)を適用します。
