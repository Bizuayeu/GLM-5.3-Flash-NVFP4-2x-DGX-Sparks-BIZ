# Business-use performance and quality catalog (BIZ)

[日本語](optimization-catalog.ja.md) · [Project purpose](../README.md#business-use-objectives-biz) · [Measurement procedures](performance-investigation.md)

This catalog records what to investigate, what has been measured, and why a candidate was retained or deferred, so the next GLM version can be evaluated against an explicit baseline. It owns initiative IDs and reevaluation criteria; linked documents own measurements, detailed test contracts and runtime settings. Listing a candidate does not mean it is implemented or available as a startup option.

The Mia-informed client authentication, allocator, all-HCA checks, two-rank switch and APC history retention extend P10/P19/P22/E03. They are not counted as another independent acceleration. See the [launch contracts](launch-safety.md) and [history-retention measurements](benchmarks.md#apc-history-retention-baseline), keeping functional acceptance, performance adoption, defaults and unqualified scope separate.

## Baseline and source ownership

**Initial baseline: 2026-09-12 (Asia/Tokyo), documents at commit `9997ccc`. Rows carry their own dates.** Ongoing integration runs and uncommitted prototypes are not passing results. Follow later results through the linked sources and Git history; retain the document commit and run ID when comparing versions.

This catalog and its Japanese counterpart own the stable IDs, intent, dated status and reevaluation criteria. Which document owns settings, measurements, acceptance contracts and storage paths is listed once in the [document map](README.md#single-sources-of-truth); configurable limits in the startup settings are not qualified limits; the [optimization overview](optimization-overview.md) reads this catalog by inference stage and workload. Private `records/<run-id>/` hold raw responses, traces, all repetitions, failures and site details; public documents contain verified summaries.

The initial reference is two GB10 nodes, TP=2, Marlin W4A16, FP8 KV, candidate-preserving NoPE reference attention, eager execution and one active sequence, with MTP/LPA/fusion/APC off. The tested W4A16 arithmetic is distinct from the original W4A4 recipe.

The observed division of benefit is primarily decode for MTP, long-input prefill for LPA, and prefill for current CUDA unpack fusion. Fusion is a first step at KV reconstruction, not completion of attention-body fusion or CUDA Graphs. CSA2 stopped at its cost gate (P16).

Current defaults: [server configuration](server-configuration.md#distributed-defaults).

## Performance initiatives

Expected effects are hypotheses. A measured result applies to its documented conditions, not automatically to production, language quality or combined configurations. Preserve IDs across model versions, including unsuccessful candidates.

| Initiative | Work | Expected effect / metric | Status / evidence |
|---|---|---|---|
| P01 Standard MTP, three speculative tokens | Use the checkpoint's BF16 draft without an external draft model; compare off/k=1/k=3 | Fewer target steps per useful token; decode gains versus draft/verify time, acceptance and extra memory | **k=3 selected from measurements, on the shipped and on the requantized checkpoint (2026-09-21).** Enabled in the template. Depths 1–5 were measured on the reference pair on ten inputs; a depth chosen from the request's acceptance history, a confidence gate on the draft (its host synchronisation costs about as much on two hosts as it saves), not sharing the first depth's sparse top-k, and a rank-local draft argmax were measured on the same inputs and not adopted. Two sequences qualified on the published option only. [MTP](speculative-decoding.md#depth-three-for-both-checkpoints-2026-09-21) |
| P02 LPA | Predict later attention inputs; skip past-token MLP and unused queries, retaining an exact tail and all-layer decode | Lower long-input prefill/TTFT; assess quality effects of approximate state, not an assumed decode gain | **Measured experimental path.** Limited long-task checks; broader quality and integration are separate gates. [LPA](lpa.md) |
| P03 CUDA fusion: KV reconstruction | Fuse FP8 MLA-cache copies, FP32 conversion and scale multiplication with Triton | Fewer launches/intermediates, primarily faster prefill | **Exact components and full-target A/B/A measured.** The gain is at prefill; little short-context decode change. Enabled in the template. [Measurements](component-validation.md) |
| P04 NoPE attention-body kernels/fusion | Replace Python query loops and multiple operations while retaining candidates, scales and causal masks | Lower prefill/decode attention time and launch count | **Reject tested larger query chunks and FP32 fusion.** Fewer launches/intermediates did not improve speed; no serving integration. Reopen with evidence for a different strategy. [Components](component-validation.md#nope-attention-fusion-and-query-batching-p04) |
| P05 SM121 attention backend selection | First verify fixed-GLM NoPE/sparse-MLA shape/cache support in candidates such as FlashInfer/TRT or Triton, then A/B matched workloads | Reliable startup/warmup and effective kernel performance; detect unintended fallback | **Adopted for prefill in 1.6.0 (`runtime.fa2_attention`, on in the template).** Calls of more than six rows go through the SM90 FA2 wrapper over rows unpacked to BF16: prefill, and with two sequences their shared decode steps; one sequence's decode steps stay on the reference computation, and LPA is excluded. Identical requests still repeat bit for bit ([prefill on 1.6.0](benchmarks.md#prefill-and-decode-on-160)). On the way there, direct SM120 substitution was not adopted: rows with few candidates miss the bound even with empty rows zeroed, and the kernel caps the width at 2,048. The SM90 FA2 wrapper passed every numerical check at full candidate width on GB10, with BF16 KV only (FlashInfer 0.6.18 rejects FP8 MLA KV off SM90). [Direct probe and revisit](component-validation.md#direct-padded-native-attention-probe) / [SM90 FA2](component-validation.md#sm90-fa2-mla-wrapper-probe) |
| P06 CUDA Graphs | Resolve host decisions, dynamic operations and buffer lifetimes for capture/replay of supported shapes | Lower CPU step/launch overhead, especially steady decode | **Not adopted after a full-model A/B (2026-09-21):** with `runtime.decode_graphs = true` on the serving profile at depth 4, every one of ten inputs decoded slower per step than eager with identical completions ([decode Graphs on the full model](benchmarks.md#decode-graphs-on-the-full-model)); the option stays off and is kept for a later runtime. Before that, on the baseline where identical requests repeat, the MTP fixture matched eager exactly (8 runs, 31 pairs; the divergence of 2026-09-12 had been the expert token order), and the first full-model launch stopped itself on the memory reserve during the prefill of the 199,652-token passphrase request (available memory 6.5 to 2.9 GiB, where eager kept 6.14) after decoding at 31.90 tok/s against 31.78 and 30.99 eager. LPA's eager restriction needs its own validation. Other two-Spark TP=2 recipes report +2–5% (sfxnz PR #1) and no change (tonyd2wild PR #16); no code adopted. [Procedure](performance-investigation.md#kernel-launches-and-synchronization) |
| P07 Launch and synchronization measurement | Separate each rank's kernels, CUDA launch APIs, NCCL and host sync; estimate per-token deltas against a prefill control | Locate bottlenecks and make before/after comparisons reproducible | **Measured.** P03 reduced launches, not NCCL count. Duration sums are not wall-clock time. [Measurements](component-validation.md) |
| P08 Further sync/copy/conversion reduction | Use P07 to locate transfers, dtype changes and host round trips; consider load-time weight repacking only after observing repeated conversion | Less avoidable latency, UMA traffic and temporary storage | **Checked async indices accepted as an independent opt-in.** Small 128-output gains, 22 fewer sync/copy events per token; 11 more GPU kernels. Distribution default async; serial combination measured under P18. [Measurements](benchmarks.md#independent-cpu-synchronization-reduction-p08) |
| P09 Independent FP8 KV evaluation | Hold weight precision fixed; compare supported FP8/BF16 cache paths, scales, capacity and readback accuracy | More context/concurrency from smaller caches; measure speed direction and quality | **FP8 in use; dtype A/B pending.** Current launch path fixes FP8. BF16 needs compatible cache layout/runtime, not just a setting. [Configuration](server-configuration.md) |
| P10 UMA and memory operation | Control KV pinning, memory utilization, host reserve, container limits, swap and cache state | Reduce OOM/swap-related latency; establish reproducibility and capacity | **Guards/settings implemented; systematic tuning pending.** Cold-cache FA2: the run-time JIT build is three translation units, so a job limit saves at most one compiler process on a new cache's first launch; the environment stays unchanged ([measurement and limits](component-validation.md#cold-cache-fa2-startup-2026-09-20)). Keeping the NVIDIA driver's JIT cache across container launches was not adopted: with only that cache empty, launches were no slower than with every cache warm (189.8 and 172.3 s against 203.9 and 197.0 s to ready, in the order cold, warm, warm, cold), with the same first-request time and memory floor; the caches already kept under `/root/.cache` account for about 80–100 s of a fully cold launch (278.3 s). Restrict `drop_caches` to justified cold-load comparisons. Supervisor records carry MemFree and free 2 MiB-or-larger blocks beside MemAvailable. `vm.swappiness` 0 against 60 showed no benefit on the 200K profile, so hosts stay at 60. [Configuration](server-configuration.md) / [benchmarks](benchmarks.md) / [swap](operations.md#supervision-stall-detection-and-warmup) |
| P11 Prefill chunk × Kpool/indexer | Vary chunk budgets; compare split/native processing at and around pool, tail and cache boundaries | Improve long prefill and mixed-load ITL while preserving state/candidates | **Default 2048; 128 rejected.** On the 200K image profile with one sequence, 1024 and 2048 raised 39K prefill and shortened the 200K passphrase request, for up to 0.7 GiB less peer headroom; with two sequences 1024 had lengthened the longest 2K pause. Sizes above 2048 unmeasured. With `kv_cache_memory_bytes` fixed, memory profiling is skipped, so the KV budget does not validate a larger chunk's activation peak: measure reserve headroom before raising it; host supervision remains the runtime protection. [Two-sequence P11](benchmarks.md#independent-prefill-chunk-evaluation-p11) / [200K profile](benchmarks.md#chunk-budget-on-the-200k-image-profile-2026-09-17) |
| P12 Concurrent-stream measurement | Record client concurrency 1/2/4 separately from server active sequences; tabulate TTFT, queue, individual ITL and aggregate | Expose serial/parallel tradeoffs and stalls during long-input arrival | **Queueing and real batching measured separately.** P13 observed two active requests and multiple decode rows. [Measurements](benchmarks.md#independent-active-batching) |
| P13 Standard batching | With LPA off, validate seqs=2 then 4 and actual overlap; separate determinism from task acceptance | More aggregate throughput; quantify per-request latency/memory tradeoffs | **Two sequences accepted.** First for the limited throughput workload (short/2K input, 64 output, with a separate 16K × two capacity check); the published option serves two since 2026-09-23 (routine use, up to about 200K tokens each), and repeatable completions need `max_num_seqs = 1` ([concurrency scope](validation.md#concurrency-scope)). Default one; more than two unqualified. Keep performance, quality and capacity scopes distinct: a configured limit does not enlarge the KV reservation. Another recipe's report on two long concurrent requests (tonyd2wild #14, no code adopted) is outside this scope. [Measurements](benchmarks.md#independent-active-batching) |
| P14 Similar-task batching | Compare code/translation/summary groups with mixed order, matching length, concurrency and offered load | Possible changes in expert overlap/speculative acceptance and aggregate throughput | **Not adopted for this workload.** One small matched order comparison gained only 0.7-0.9%; no expert-route attribution or complete-task quality evidence. Retain standard batching. [Measurements](benchmarks.md#task-grouping-order-comparison-p14) |
| P15 Context-length sweep | Vary input length and KV budget independently; consider 8K/32K/128K/262,144 only after capacity/support checks | Map prefill/decode/memory breakpoints and usable limits | **Serial capacity/latency through 32K measured with fixed KV 1 GiB per rank.** All 30 measured requests completed without preemption. Long-task quality and combinations remain separate; [256K combined checks](benchmarks.md#real-input-checks-at-256k) use a different KV budget. [Measured sweep](benchmarks.md#independent-context-sweep-through-32k-p15) |
| P16 CSA2: candidate Reuse/Reindex | Compare cross-layer selection reuse, restricted rescoring and shared pools, preserving per-layer KV and mandatory writes | Potential reduction in removable indexer work, judged by coverage and full latency | **Stopped at the cost gate (2026-09-21).** The indexer's share of prefill, on the four-layer fixture and projected for the full model at 200K, is below the design's first acceptance gate, so no reuse was built. Components retained. [Design and result](indexer-reuse.md) / [earlier observation](component-validation.md#indexer-observation) |
| P17 TP=2 versus PP=2 | Implement/fixture-test intermediate tensors and mHC post/comb transport before matched A/B | Possible communication-wait savings versus stage serialization/imbalance | **Not adopted for the measured generation workload; default TP2.** Prefill improves, decode slows. Timed A/B/A and scoped task/cancel checks completed; PP profiler restart failed, so PP capacity and decode trace remain incomplete. [Measurements](benchmarks.md#independent-tp2-versus-pp2-evaluation-p17) |
| P18 MTP + LPA + fusion, optionally Graphs | Compare individual/combined paths on identical assets/tasks with restored controls and observed LPA/speculation/capture activity | Determine interaction, quality/memory regressions and recovery | **Serial MTP3/LPA/fusion/async combination accepted within measured scope.** Paired 2K/8K timing, 24 tasks, tools, 16K capacity and active-LPA disconnect completed. Graphs/batching and business-use qualification remain separate. [Integration results](benchmarks.md#serial-integration-of-mtp-lpa-fused-unpack-and-async-checks-p18) |
| P19 Prefix caching (APC) | Compare cold/warm repeated system/tools/history and different prefixes; check hybrid state, boundaries and isolation | Lower TTFT and repeated prefill in conversations | **Accepted for measured serial long-prefix reuse, enabled in the server template.** Full-model A/B/A, real hits, long-answer isolation, tool and cancellation passed. Cold processing was slightly slower. LPA coexistence is P22. [Full-model evidence](benchmarks.md#independent-full-model-prefix-caching-p19) |
| P20 Indexer workspace sizing | Measure peak requirements by shape/chunk/MTP depth; shrink only proven excess reservation | More host/KV headroom and fewer unnecessary allocations | **Conditional candidate; independent benefit unqualified.** Distinct from P16 selection changes; omit patches superseded upstream. |

**Additional initiative (2026-09-13):**

| Initiative | Work | Expected effect / metric | Current status / procedure |
|---|---|---|---|
| P21 Expert Parallel | Independently switch expert-layer partitioning from TP to EP while retaining TP=2, DP=1, two active sequences and the fixed per-rank KV budget. Check pinned GB10/Marlin support before implementing default-off configuration, launch and validation paths | Expert compute efficiency and aggregate throughput, assessed with extra memory, communication, individual TTFT/ITL and quality | **Not adopted for measured performance; default off.** All four full-model A/B/A cases were slower than both off controls. Scoped task/cancel/16K x two capacity checks passed; other workloads/combinations need separate qualification. [Measurements](benchmarks.md#independent-expert-parallel-evaluation-p21) |
| P22 APC priority with LPA on the uncached suffix | Decide from jointly restored H and eligible work max(0,N-T-H); share only ordinarily computed states and suppress publication from the first approximation onward | Reuse exact prefixes, accelerate long residual prefill, and prevent approximate-state contamination across requests | **Standalone calibration, scoped quality/operations, MTP/fusion/async integration and final held-out checks completed.** Prefer the no-MTP profile for the measured prefix-reuse workload. [Contract](apc-lpa-design.md) / [Profile tradeoff](benchmarks.md#repeated-input-tradeoff) |

**Additional initiative (2026-09-16):**

| Initiative | Work | Expected effect / metric | Current status / procedure |
|---|---|---|---|
| P23 W4A16 repack of the projections NVFP4 leaves in BF16 | Offline W4A16 NVFP4 repack of the attention projections and `lm_head` (route l), served through `runtime.derived_checkpoint`. Keep the NVFP4 experts, the pinned kernels and the MTP draft unchanged | Less weight traffic per decode step on projections that are read for every token; measure decode tok/s at short and long input, TTFT and memory | **Adopted as a published option for Japanese prose (2026-09-21); not in the defaults, because it is not lossless.** Weights on Hugging Face ([Bizuayeu/GLM-5.3-Flash-NVFP4-attn-lmhead-W4A16](https://huggingface.co/Bizuayeu/GLM-5.3-Flash-NVFP4-attn-lmhead-W4A16)). Against route g (the attention projections alone, served 2026-09-19 to 21 at depth 4 and superseded), adding `lm_head` shortened the decode step by 12–13 ms on every input (the BF16 `lm_head` is read once per draft depth) and moved teacher-forced NLL by +0.27% on Japanese, +0.02% on English, −0.09% on code and +1.53% on mathematics; the 200K passphrase and the 261,461-token three-position reference were answered correctly ([serving profile](benchmarks.md#the-reference-pairs-serving-profile-attention-and-lm_head-repacked-depth-3)). **Route g**, off/on/off after the expert order fix and with FA2 prefill: decode over nine samples rose from 32.87 to 42.39 tok/s on counting (+29%), 20.82 to 25.31 on prose (+22%) and 27.62 to 34.93 on code (+27%) with the acceptance length unchanged, the two unmodified launches within 1%; weights 4.0 GiB smaller per rank, the head kept at least 9.52 GiB; NLL rose 4.0% on Japanese, 5.9% on code and 4.3% on mathematics and fell 1.1% on English. Adding the shared experts (2026-09-20) was not adopted: 0.68 GiB smaller per rank and counting +3.9%, against code −10.2%, short prompts −23% and prose −2.3% as the draft's acceptance length fell. **Prefill:** the fused KDA input projection under W4A16 Marlin cost 1.48 times a BF16 GEMM at 2,048-row chunks (read at a per-rank width of 12,416 columns without the output slice), and the option prefilled slower than the defaults on the same night ([measurements on 1.7.1](benchmarks.md#measurements-on-171)). Marlin's cost is the GEMM width, so 1.8.0 splits that projection into `q_proj`, `k_proj`, `v_proj` and a `b`/`f_a`/`g_a` tail, which prefills faster than the fused layout and than the defaults with decode, memory and numerics unchanged ([measurements on 1.8.0](benchmarks.md#measurements-on-180)). **First reading (2026-09-18, closed then, reopened above):** on a yardstick that moved 11% between identical runs, decode was 26.96 tok/s against 24.91 and 26.21 unmodified (spread 24.35–34.44) as the mean acceptance length fell from 2.9 to 2.37, mathematics NLL rose about 5% and argmax agreement with a reference run was 0.909 against 0.946–0.962 between unmodified runs. The method had moved from load-time FP8 to tenhkspark's route g, the same Marlin arithmetic as the experts. On the four-layer fixture the 33 repacked tensors have a relative error of 0.091–0.095 and the layer-3 candidate sets overlap the unmodified fixture at Jaccard 0.950 (0.948 on the eight-layer fixture's second MLA layer), so the shift does not compound across MLA layers ([requantization checks](validation.md#full-model-tp2-experimental-scope)). **Sources, no code adopted:** Mia PR #139 (the dense MLP layers it targets are already NVFP4 here; its adaptive verification length stays rejected, since eager MTP k=3 has no CUDA graph to preserve); tonyd2wild's recipe reached the same tensor set independently on TP=4 (commit `9fea48c`, ×1.14–1.30 per task there, quality unmeasured); Mia's KDA large-M BF16 path (#233) addresses the same prefill mechanism on FP8-Marlin |

P12 is a measurement axis, P13 a scheduler setting, and P14 a workload-order experiment: do not count one gain three times. MTP can already verify multiple speculative rows within one sequence; GEMM-like work does not necessarily require `max_num_seqs > 1`.

**Additional initiative (2026-09-17):**

| Initiative | Work | Expected effect / metric | Current status / procedure |
|---|---|---|---|
| P24 Per-request prefix-cache no-store | Let a request opt out of *writing* the GPU prefix cache while still reading it. The pool frees a hashed block to the back of the free queue and an unhashed one to the front, and allocation pops from the front, so lanes that never store recycle each other's blocks instead of pushing an idle conversation out. Needs a runtime overlay in the existing house style (a typed sampling field plus guards at the two request-driven store sites), an image rebuild and a launcher knob; no new dependency | Retained hit ratio and turn latency of a long conversation while other lanes run; the flagged lane must keep its own reads | **Candidate, measured need and measured scope, not started.** Measured on the 1.4.0 image profile at 2.5 GiB KV (not re-measured at 3 or 6 GiB) against an 80,024-token conversation warm at 73,728 cached tokens and 14.9 s per turn. **What no-store would fix:** individually harmless lanes accumulate: two untouched 15,025-token lanes left the owner intact, four evicted it completely (next turn 182.6 s). **What it would not fix:** a lane whose own allocation exceeds the free space: one 42,026-token lane evicted the owner on its own (next turn 184.0 s), while single lanes of 9,025 and 18,025 tokens left it intact (15.0 s), so the single-lane boundary lies between 18K and 42K. On that profile the owner alone nearly filled the pool ([block accounting](server-configuration.md#kv-capacity-and-ram-requirements)); the per-group accounting is not established, which is why `server capacity` withholds its conversation figure. Informed by Mia PR #95 (no code adopted), whose receipts needed sixteen ~54K lanes on a larger pool. Until it exists, keep lanes beside a long interactive conversation small and few |
| P25 Prefix-cache page dedup under MTP | Do not register a full block under a hash that already has a cached block. Source-pinned patch on `vllm/v1/core/block_pool.py` (`_insert_block_hash`), profile key `runtime.prefix_page_dedup` (absent = off), image marker `GLM53_PREFIX_DEDUP_API=1`, preflight row; no new dependency | Keep older histories cached while one history is re-sent many times under a draft: each re-send added one block per KV cache group under an existing hash (the draft's prefix lookup drops and recomputes the last matching block) | **Adopted in 1.9.0; set in the serving profile of the reference pair, not in the template (2026-09-22).** Fixture: duplicates zero, hits and completions identical to the pinned pool. Reference pair: a 57K-token history re-sent fifteen times evicted a 28K-token one with the option off and left it cached with it on; decode and prefill unchanged ([measurements on 1.9.0](benchmarks.md#measurements-on-190)). Informed by vLLM issue #53670 (no code adopted) |
| P26 Sparse-MLA decode split per sequence, and the batch-invariance survey | Take FlashInfer's `chunks_per_block` for the SM120 sparse-MLA decode from the tokens of one sequence (2 per draft step, 6 per verification step) and call its decode entry with it. Source-pinned patch on the SM120 backend, profile key `runtime.mla_decode_cpb` (absent = off), image marker `GLM53_MLA_DECODE_CPB_API=1` (required with the key), preflight row; no new dependency. Surveyed alongside: padding the Marlin MoE to whole tiles, and running decode and prefill rows of a shared step separately | A request's attention no longer depends on the other requests in its decode step; a request alone computes as before | **Retired in 1.16.0: never reached in serving (2026-09-26).** Adopted in 1.14.0 in both examples on a kernel harness that set the backend's reference flag off (there every attention row of a request changed with a partner without the key, none with it). The image sets `GLM53_REFERENCE_ATTENTION=1`, so every sparse-MLA forward returns through the reference attention before the patched call; the key had no effect, and the 1.14.0 pair results (lone completions, decode speed, NLL and long inputs unchanged) are consistent with that ([reachability in serving](benchmarks.md#reachability-in-serving-2026-09-26)). The examples drop the key; a profile that carries it still launches, with a warning; the patch and marker go with the next image build. The survey stands: two-sequence completions still differ from lone ones, the attention's switch to FA2 with a partner is not the main cause and the MoE is the lead suspect, so repeatable completions come from `max_num_seqs = 1` ([measurements on 1.14.0](benchmarks.md#measurements-on-1140)). Not adopted: the MoE padding (it removes the MoE's dependence at a cost on every MoE call) and the split step (MoE +0.5 to 2.1 ms per layer on a shared step); neither completes two-sequence repeatability alone. On 2026-09-26 the serving pair was tried with the Marlin MoE split pinned for decode-sized calls, the shared-expert and router GEMMs computed per sequence, and FA2 off: the switches engaged, but all eight two-sequence completions still differed from their lone ones. Not adopted; repeatability stays with `max_num_seqs = 1` (the distributed default), and the published option keeps two sequences. Reported upstream as flashinfer#5553 (FlashInfer's own behaviour, which this serving does not reach) and on vllm#46639 |

## Business-use quality and adoption gates

Evaluate performance together with source-faithful answers, correct tools and manageable licensing. The objective is neither to steer the model toward a particular ideology nor to remove all appropriate safety refusals.

| Initiative | Work | Expected benefit / evidence | Status / source |
|---|---|---|---|
| E01 License and provenance selection | Separate model, original code, patches, containers and harnesses; pin versions/notices under the MIT/Apache-centered selection policy | Make usage, modification and distribution boundaries reviewable by adopting organizations | Policy and inventory available; containers and harnesses do not acquire one blanket license. [Licensing](licensing.md) / [notices](../THIRD_PARTY_NOTICES.md) |
| E02 FreedomBench and political context | Separate original English, Japanese and long business-context tests across baseline/MTP/LPA/combined profiles; distinguish refusal, wrong answer, format and transport failures | Reveal responses to tested topics, unsupported political insertions, source infidelity and changes caused by optimization | Closed for the serving profile; untested extensions are listed in [FreedomBench](freedombench.md#closure-on-the-serving-profile-2026-09-22) |
| E03 Business functionality and operation | Test text, tool round trips, SSE, cancellation, isolation, ZCode/Claude Code, sustained load and two-rank recovery | Determine whether a fast benchmark profile is usable in practice; preserve rollback | Routine use accepted ([SETUP step 6](../SETUP.md#6-qualify-the-full-model)); production reliability not validated. [Validation](validation.md) / [harnesses](harnesses.md) / [operations](operations.md) |

FreedomBench measures agreement with its authors' answers on a limited set of questions. **A high score cannot prove the absence of all ideological bias.** Provide scope, conditions, results and untested areas so adopting organizations can judge suitability for their own use. Do not mix translated/extended scores with the original, or count short questions on which LPA did not activate as evidence for approximation quality. Reevaluate with the effective fusion/graph settings when adding those paths. The FreedomBench documents alone own question and scoring contracts.

## Repeatable process for the next version

1. **Establish a new native reference.** Check model/revision, licenses, runtime, SM support, quantization and cache layouts. Reassess old patches; mark upstream-resolved work unnecessary rather than automatically porting it.
2. **Move from components to the full model.** Separate exact arithmetic tests from approximation quality. Validate fixture state, then matched full-model A/B/A; separate tracing from final timing.
3. **Decide on each component, then combine at the end.** Evaluate each initiative independently through an adopt/defer/reject decision. Hold prerequisite components fixed in both arms and change one initiative per comparison. Once candidates are selected, use P18 for intended operating combinations and necessary off controls. Do not repeat the entire integration matrix after every component; retain existing combined runs as preliminary evidence. Do not simultaneously change batching, long-context capacity or PP. Later runs do not necessarily share the same image, inputs or resource budgets: **do not add or multiply gains from different runs.**
4. **Separate functional acceptance from performance adoption.** Infrastructure such as Graphs may be accepted with little speed gain once correctness, resource limits and recovery are verified within its supported scope. Adoption as an acceleration requires a reproducible gain. Decide default enablement and final integration qualification separately. Distinguish exact text, task/tool correctness, refusals and incomplete execution; establish workload/thresholds before results. E02/E03 precede business-use qualification.
5. **Retain negative decisions.** Record noise-level effects, slowdowns, capacity/quality failures or upstream obsolescence with reopening criteria. Preserve old results in Git and create new runs instead of overwriting them.

### Functional acceptance and defaults

Record **functional acceptance with its tested scope / performance adoption / default on or off / integration pending or qualified** separately. “Functionally accepted; speed difference within noise; default off; integration pending” is a valid outcome. Component or truncated-fixture success does not qualify the full model.

For P06, check memory retained for graphs, startup capture time, supported shapes and buffer lifetimes, and recovery from failures. Graphs is not a side-effect-free setting. Before adopting graphs, also check that per-position speculative acceptance is not pinned at exactly 1.00: with CUDA graphs that reading is the vllm-project/vllm#53030 signature (a post-boot gate is described in Mia PR #70), not a real acceptance rate. Check LPA prefill and MTP speculative paths in P18; speed alone does not justify graphing every path. See PyTorch's [CUDA Graph constraints and memory management](https://docs.pytorch.org/docs/main/notes/cuda.html#cuda-graphs).

### Shared comparison record

Use these fields in the next version's experiment report; this is not a new configuration format or automated scoring system.

| Field | Record |
|---|---|
| Target and decision | Initiative ID, old/new version, intended workload/metric, predeclared criteria, adopt/defer/reject and reason; separate functional scope, performance adoption, defaults and integration qualification |
| Fixed assets | Model revision, source commit, both rank images, runtime/backend, arithmetic, template/tokenizer, projector/view and license inventory reference |
| Effective configuration | Profile fingerprint, TP/PP, active sequences, chunk, KV dtype/capacity, context, actual MTP/LPA/fusion/graph/APC state |
| Workload | Corpus/task/question hash and split, request IDs, actual input/output tokens, effort/sampling, order/concurrency/actual overlap and other load |
| Timing | Cold startup, warmup, TTFT, prefill control, per-request TPOT/ITL/queue/E2E, aggregate, repetition count and distribution; small-sample medians are not an SLA |
| Attribution and resources | Per-rank kernel/launch/NCCL/sync/copy, MTP mean accepted length and steps, minimum host reserve with MemFree and free 2 MiB-or-larger blocks, container peak, swap and temperature; do not sum overlapping intervals |
| Quality and completeness | Planned/completed/missing/error counts, refusals, truncation, content/tools/source fidelity, numerical/state diagnostics and restored controls; identify unqualified areas |
| Reproduction and source | All A/B/A trials, profiler-off timing, raw run ID, reference document commit, public summary links and rollback settings |

When tokenizers or input lengths change, separate same-text business comparisons from same-token-budget processing comparisons. Keep raw logs private and maintain links from this catalog to the authoritative result documents.
